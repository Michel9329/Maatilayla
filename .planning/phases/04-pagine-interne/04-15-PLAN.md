---
phase: 04-pagine-interne
plan: 15
type: execute
wave: 2
depends_on: ["04-14"]
files_modified:
  - src/pages/BlogArticle.tsx
  - src/components/sections/ArticleSidebar.tsx
  - src/components/sections/ArticleCta.tsx
  - src/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "L'articolo singolo ha un hero compatto con foto full-width, breadcrumb cliccabili, titolo, data e reading time"
    - "La sidebar sticky mostra TOC generato da H2, articoli correlati, tag, CTA contatti e foto Instagram"
    - "In fondo all'articolo c'e' una CTA con foto background (pattern ChiSiamoCta)"
    - "Su mobile la sidebar appare sotto il contenuto articolo"
  artifacts:
    - path: "src/pages/BlogArticle.tsx"
      provides: "Pagina articolo redesign con hero compatto + layout 2 colonne"
      min_lines: 80
    - path: "src/components/sections/ArticleSidebar.tsx"
      provides: "Sidebar sticky con TOC, correlati, tag, CTA, Instagram"
      min_lines: 80
    - path: "src/components/sections/ArticleCta.tsx"
      provides: "CTA fine articolo con foto background"
      min_lines: 50
    - path: "src/index.css"
      provides: "CSS as- per sidebar, ar-hero per hero compatto, ac- per CTA articolo"
      contains: ".as-sidebar"
  key_links:
    - from: "src/pages/BlogArticle.tsx"
      to: "src/components/sections/ArticleSidebar.tsx"
      via: "import e rendering nella colonna destra del layout"
      pattern: "ArticleSidebar"
    - from: "src/components/sections/ArticleSidebar.tsx"
      to: "src/components/sections/ArticleRenderer.tsx"
      via: "TOC ancora links puntano agli id generati da ArticleRenderer"
      pattern: "scrollIntoView|href.*#"
    - from: "src/pages/BlogArticle.tsx"
      to: "src/components/sections/ArticleCta.tsx"
      via: "import e rendering dopo article content, prima della nav prev/next"
      pattern: "ArticleCta"
---

<objective>
Ridisegnare la pagina articolo singolo (/blog/:slug) con: hero compatto (foto + breadcrumb + titolo + meta), layout a 2 colonne (articolo + sidebar sticky), e CTA fine articolo con foto background.

Purpose: Trasformare la pagina articolo da layout semplice a esperienza editoriale completa con navigazione TOC, contenuti correlati e call-to-action.
Output: BlogArticle.tsx ridisegnato, ArticleSidebar.tsx, ArticleCta.tsx, CSS completo.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pagine-interne/04-BLOG-REWORK-CONTEXT.md

@src/pages/BlogArticle.tsx
@src/components/sections/ArticleRenderer.tsx
@src/components/sections/ChiSiamoCta.tsx
@src/data/blogArticles.ts
@src/index.css

<interfaces>
From src/pages/BlogArticle.tsx:
```typescript
// useParams -> slug -> getArticleBySlug(slug)
// blogArticlesSorted per prev/next navigation
// Helmet SEO + JSON-LD gia' presenti — mantenere
// Struttura attuale: ba-article > ba-header > ba-cover > ArticleRenderer > ba-nav
```

From src/data/blogArticles.ts:
```typescript
export interface BlogArticle {
  slug: string; title: string; date: string; category: string;
  excerpt: string; image?: string; imageAlt?: string;
  imagePosition?: string; content: string; readingTime: number;
}
export function getArticleBySlug(slug: string): BlogArticle | undefined
export const blogArticlesSorted: BlogArticle[]  // ordinati per data decrescente
export const blogCategories: string[]
```

From src/components/sections/HeroSection (gia' supporta compact):
```typescript
interface HeroProps {
  image: string; alt: string; title: ReactNode; subtitle?: string;
  compact?: boolean; lightText?: boolean;
  // compact riduce altezza hero
}
```

Pattern CTA (ChiSiamoCta): bc-wrap > bc-section > bc-content + img.bc-bg
Per ArticleCta usare prefisso ac-.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ridisegnare BlogArticle.tsx con hero compatto e layout 2 colonne</name>
  <files>src/pages/BlogArticle.tsx, src/index.css</files>
  <action>
1. Ridisegnare `src/pages/BlogArticle.tsx`:

   **Hero compatto** (sostituisce ba-header + ba-cover attuali):
   - Usare HeroSection con `compact={true}` se l'articolo ha immagine, altrimenti header semplice
   - Per articoli CON immagine: HeroSection compact con foto articolo come sfondo, overlay scuro per leggibilita'
   - Sovrapporre al hero (posizionato in basso, sopra l'overlay):
     - Breadcrumb: `Home > Blog > {titolo troncato a ~40 chars}` — Home e Blog sono Link cliccabili, titolo no
     - Titolo H1 (colore bianco su overlay scuro)
     - Riga meta: categoria (pill verde salvia) + data formattata + reading time
   - Per articoli SENZA immagine: header semplice su sfondo cream con breadcrumb + titolo + meta (senza HeroSection)
   - NOTA: il contenuto sovrapposto all'hero va realizzato con un div posizionato absolute/relative dentro il wrapper hero, NON come props di HeroSection (che non supporta children arbitrari). In alternativa, creare un hero custom inline nel componente con le stesse classi CSS.

   **Layout 2 colonne** (sotto l'hero):
   - Wrapper `.ba-layout` con display grid: `grid-template-columns: 1fr 320px` su desktop (min-width 1024px)
   - Colonna sinistra `.ba-main`: ArticleRenderer + ArticleCta + nav prev/next
   - Colonna destra `.ba-aside`: ArticleSidebar (importato)
   - Mobile (sotto 1024px): colonna singola, sidebar sotto articolo

   **Mantenere invariati:**
   - Helmet SEO + JSON-LD
   - Prev/next navigation (ba-nav)
   - Navigate redirect su slug non trovato

2. CSS in `src/index.css`:
   - `.ba-hero-custom`: position relative, min-height clamp(280px, 35vh, 400px), overflow hidden, border-radius 0 (full width)
   - `.ba-hero-bg`: position absolute, inset 0, object-fit cover, width/height 100%
   - `.ba-hero-overlay`: position absolute, inset 0, background linear-gradient(to top, rgba(0,0,0,0.7) 0%, rgba(0,0,0,0.3) 100%)
   - `.ba-hero-content`: position absolute, bottom 0, left 0, right 0, padding 2rem clamp(1.5rem, 5vw, 4rem), z-index 1, max-width 900px
   - `.ba-breadcrumb`: display flex, gap 0.5rem, font-size 0.8rem, color rgba(255,255,255,0.7), margin-bottom 1rem
   - `.ba-breadcrumb a`: color rgba(255,255,255,0.85), hover underline
   - `.ba-breadcrumb-sep`: color rgba(255,255,255,0.4) (carattere ">")
   - `.ba-hero-title`: font-family var(--font-heading), font-size clamp(1.6rem, 4vw, 2.8rem), font-weight 700, color white, line-height 1.2, margin-bottom 0.75rem
   - `.ba-hero-meta`: display flex, gap 1rem, align-items center, flex-wrap wrap
   - `.ba-hero-category`: background rgba(139,173,145,0.25), color #c5e6cb, padding 0.2rem 0.7rem, border-radius 1rem, font-size 0.75rem, font-weight 600
   - `.ba-hero-date`, `.ba-hero-readtime`: color rgba(255,255,255,0.7), font-size 0.8rem
   - `.ba-layout`: max-width 1200px, margin 0 auto, padding 3rem clamp(1.5rem, 5vw, 4rem), display grid, grid-template-columns 1fr, gap 3rem
   - Desktop 1024px+: `.ba-layout` grid-template-columns: 1fr 320px
   - `.ba-main`: min-width 0 (prevent grid blowout)
   - `.ba-aside`: (wrapper per sidebar)
   - Rimuovere o rinominare i vecchi stili `.ba-header`, `.ba-cover`, `.ba-back` che non servono piu'
   - Mantenere `.ba-nav` (prev/next navigation) con eventuali aggiustamenti di larghezza
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pagina articolo ha hero compatto con breadcrumb/titolo/meta + layout 2 colonne su desktop + colonna singola su mobile</done>
</task>

<task type="auto">
  <name>Task 2: Creare ArticleSidebar + ArticleCta + CSS</name>
  <files>src/components/sections/ArticleSidebar.tsx, src/components/sections/ArticleCta.tsx, src/index.css</files>
  <action>
1. Creare `src/components/sections/ArticleSidebar.tsx`:
   - Props: `{ article: BlogArticle, headings: Array<{id: string, text: string, level: number}> }`
   - Il componente riceve headings gia' estratti (BlogArticle.tsx li estrae dal content markdown con regex `^#{2,3}\s+(.+)$` e li passa)

   **Sezioni sidebar (dall'alto in basso):**

   a) **TOC — Sommario** (`.as-toc`):
      - Titolo "Sommario" in grassetto
      - Lista heading H2 come link anchor (`<a href="#{id}">`)
      - Click: smooth scroll alla sezione (`element.scrollIntoView({ behavior: 'smooth', block: 'start' })`)
      - Active state: evidenzia heading corrente basato su scroll position (IntersectionObserver sugli heading dell'articolo)
      - Se nessun heading: non renderizzare la sezione TOC

   b) **Articoli correlati** (`.as-related`):
      - Titolo "Articoli correlati"
      - 2-3 card compatte: thumbnail piccola (48x48 o 64x48, border-radius 0.5rem) + titolo (line-clamp 2) + data
      - Algoritmo: stessa categoria dell'articolo corrente, escluso l'articolo corrente, ordinati per data, max 3. Se meno di 2 nella stessa categoria, aggiungere articoli recenti di altre categorie
      - Link to={`/blog/${slug}`}

   c) **Categorie** (`.as-tags`):
      - Titolo "Categorie"
      - Pill cliccabili con le categorie blog (Link to={`/blog?cat=${encodeURIComponent(cat)}`} oppure semplice Link to="/blog")
      - Pill attiva (categoria dell'articolo corrente) evidenziata in verde salvia pieno, altre in outline

   d) **CTA Contatti** (`.as-cta-card`):
      - Card compatta con sfondo cream
      - Titolo: "Hai domande?"
      - Body breve: "Contattaci per saperne di piu' sui nostri barboncini."
      - Bottone piccolo: "Scrivici" -> Link to="/contatti"
      - Bordo sinistro verde salvia (4px)

   e) **Instagram** (`.as-instagram`):
      - Foto (una delle foto Instagram gia' in uso nel sito, es. prima della marquee)
      - Overlay gradient scuro con icona Instagram (Lucide `Instagram`) + "Seguici su Instagram"
      - Link esterno a https://www.instagram.com/maatilayla (target _blank, rel noopener)
      - Border-radius var(--radius-card), overflow hidden
      - Hover: leggero zoom foto (scale 1.03)

   **Sticky behavior:**
   - Wrapper `.as-sidebar` con `position: sticky`, `top: 6rem` (offset per navbar fixed), `align-self: start`
   - Max-height `calc(100vh - 8rem)`, overflow-y auto (con scrollbar sottile custom)

   **Entrata:**
   - CSS transition entrance: fade-in 0.5s con delay 0.2s via IntersectionObserver (classe `as-entered`)

2. Creare `src/components/sections/ArticleCta.tsx`:
   - Pattern identico a BlogCta (bc-) ma con prefisso `ac-` e contenuto diverso
   - Wrapper `.ac-wrap`, section `.ac-section` con foto background
   - Foto: riusare una foto barboncini disponibile (diversa da BlogCta se possibile)
   - Badge: "Contatti" con dot pulse
   - Titolo word-by-word: "Vuoi saperne di piu'?" con "piu'?" come accent
   - Body: "Siamo sempre disponibili per rispondere alle tue domande sui nostri barboncini toy."
   - CTA button: "Contattaci" -> Link to="/contatti"
   - Desktop: GSAP word-by-word (ScrollTrigger), Mobile: IntersectionObserver CSS
   - prefers-reduced-motion gestito
   - Dimensioni piu' contenute rispetto a BlogCta: max-width 900px (sta dentro ba-main), min-height 320px

3. CSS in `src/index.css` con prefissi `as-` e `ac-`:

   **Sidebar (as-):**
   - `.as-sidebar`: position sticky, top 6rem, align-self start, display flex, flex-direction column, gap 1.5rem
   - `.as-section-title`: font-family var(--font-heading), font-size 1rem, font-weight 700, color var(--color-text), margin-bottom 0.75rem
   - `.as-toc`: (nessun bordo, pulito)
   - `.as-toc-link`: display block, padding 0.35rem 0, font-size 0.85rem, color var(--color-text-muted), border-left 2px solid transparent, padding-left 0.75rem, transition color 0.2s
   - `.as-toc-link:hover`: color var(--color-text)
   - `.as-toc-link.active`: color #5A8C62, border-left-color #8BAD91, font-weight 600
   - `.as-related-card`: display flex, gap 0.75rem, padding 0.5rem 0, border-bottom 1px solid rgba(0,0,0,0.06)
   - `.as-related-thumb`: width 64px, height 48px, border-radius 0.5rem, object-fit cover, flex-shrink 0
   - `.as-related-info`: flex 1, min-width 0
   - `.as-related-title`: font-size 0.85rem, font-weight 600, line-height 1.3, -webkit-line-clamp 2
   - `.as-related-date`: font-size 0.75rem, color var(--color-text-muted)
   - `.as-tag-pill`: inline-block, padding 0.25rem 0.7rem, border-radius 1rem, font-size 0.75rem, font-weight 500, border 1px solid rgba(139,173,145,0.4), color #5A8C62, margin 0.25rem
   - `.as-tag-pill.active`: background #8BAD91, color white, border-color #8BAD91
   - `.as-cta-card`: background var(--color-cream), border-radius var(--radius-card), padding 1.25rem, border-left 4px solid #8BAD91
   - `.as-cta-title`: font-family var(--font-heading), font-size 1.1rem, font-weight 700, margin-bottom 0.5rem
   - `.as-cta-body`: font-size 0.85rem, color var(--color-text-muted), line-height 1.5, margin-bottom 0.75rem
   - `.as-cta-btn`: inline-block, padding 0.5rem 1.25rem, border-radius var(--radius-btn), background var(--color-primary), color white, font-weight 600, font-size 0.8rem
   - `.as-instagram`: position relative, border-radius var(--radius-card), overflow hidden, aspect-ratio 4/3
   - `.as-instagram img`: object-fit cover, width 100%, height 100%, transition transform 0.3s
   - `.as-instagram:hover img`: transform scale(1.03)
   - `.as-instagram-overlay`: position absolute, inset 0, background linear-gradient(to top, rgba(0,0,0,0.6) 0%, transparent 60%), display flex, align-items flex-end, padding 1rem
   - `.as-instagram-label`: color white, font-weight 600, font-size 0.85rem, display flex, align-items center, gap 0.5rem
   - Mobile (sotto 1024px): `.as-sidebar` position static, margin-top 2rem

   **ArticleCta (ac-):**
   - Stessa struttura CSS di bc- ma con prefisso ac- e max-width 900px, min-height 320px
   - `.ac-wrap`: padding 2rem 0 (dentro ba-main, no padding laterale extra)
   - Rest: stessa struttura di bc- (section, bg, overlay, content, badge, title, word, body, cta-btn)
   - Responsive: min-height 280px mobile

4. Aggiornare `BlogArticle.tsx` per:
   - Estrarre headings dal markdown content con regex e passarli ad ArticleSidebar
   - Importare e renderizzare ArticleSidebar nella colonna `.ba-aside`
   - Importare e renderizzare ArticleCta dopo ArticleRenderer, prima di ba-nav
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Sidebar sticky con TOC/correlati/tag/CTA/Instagram visibile su desktop, sotto articolo su mobile. ArticleCta con foto background prima della nav prev/next.</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- Layout 2 colonne su viewport >= 1024px
- Sidebar sticky segue lo scroll, si ferma a fine articolo
- TOC link ancorano correttamente ai heading nell'articolo
- Mobile: sidebar sotto articolo, non sticky
- ArticleCta renderizza con word-by-word GSAP su desktop
</verification>

<success_criteria>
Pagina articolo singolo ha: hero compatto con breadcrumb e meta, layout a 2 colonne con sidebar sticky (TOC + correlati + tag + CTA + Instagram), CTA fine articolo. Mobile responsive con sidebar sotto il contenuto.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-15-SUMMARY.md`
</output>
