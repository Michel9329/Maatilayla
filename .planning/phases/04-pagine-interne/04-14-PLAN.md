---
phase: 04-pagine-interne
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - content/testi-sito-attuale/blog/chi-alleva-alleva-tutto.md
  - content/testi-sito-attuale/blog/controlli-si-grazie.md
  - content/testi-sito-attuale/blog/facciamo-fare-il-cane-al-cane.md
  - content/testi-sito-attuale/blog/il-barbone.md
  - content/testi-sito-attuale/blog/il-coraggio-di-lasciarli-andare.md
  - content/testi-sito-attuale/blog/lorigine-del-nome.md
  - content/testi-sito-attuale/blog/maltipoo-cockapoo-o-goldendoodle.md
  - content/testi-sito-attuale/blog/maschio-o-femmina.md
  - content/testi-sito-attuale/blog/pandemia-sei-tutta-mia.md
  - content/testi-sito-attuale/blog/per-una-zampata-di-fango.md
  - content/testi-sito-attuale/blog/quando-a-volte-tornano.md
  - content/testi-sito-attuale/blog/quando-cerchiamo-qualcuno-che-ci-scalda-il-cuore.md
  - content/testi-sito-attuale/blog/siamo-tutti-allevatori.md
  - content/testi-sito-attuale/blog/un-cucciolo-per-natale.md
  - content/testi-sito-attuale/blog/va-dove-ti-porta-il-cane.md
  - src/components/sections/ArticleRenderer.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Tutti i 15 articoli .md hanno almeno 2 heading H2 ciascuno"
    - "ArticleRenderer genera id attributi su h2/h3 per anchor links"
    - "L'utente ha approvato le headlines proposte"
  artifacts:
    - path: "content/testi-sito-attuale/blog/*.md"
      provides: "15 articoli con H2 headlines per leggibilita' e TOC"
    - path: "src/components/sections/ArticleRenderer.tsx"
      provides: "Heading ID generation per anchor links TOC"
      contains: "id="
  key_links:
    - from: "src/components/sections/ArticleRenderer.tsx"
      to: "content/testi-sito-attuale/blog/*.md"
      via: "react-markdown renderizza H2 con id per TOC sidebar"
      pattern: "slugify|id"
---

<objective>
Aggiungere H2 headlines a tutti i 15 articoli blog per leggibilita' e generazione TOC, e aggiornare ArticleRenderer per generare id attributi sui heading.

Purpose: Gli articoli attualmente non hanno heading H2/H3 — servono sia per la leggibilita' del contenuto che per generare il sommario (TOC) nella sidebar sticky del piano 04-15.
Output: 15 file .md aggiornati con headlines, ArticleRenderer con heading IDs.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pagine-interne/04-BLOG-REWORK-CONTEXT.md

@src/components/sections/ArticleRenderer.tsx
@src/data/blogArticles.ts

<interfaces>
From src/components/sections/ArticleRenderer.tsx:
```typescript
// Custom components per react-markdown: h2, h3, p, strong, em, a, ul, li
// Rimuove H1 e riga data con regex
// h2/h3 attualmente NON hanno id — servono per TOC anchor links
```

Articoli senza heading (13/15): tutti tranne il-barbone.md (1 H2) e va-dove-ti-porta-il-cane.md (2 H2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proporre H2 headlines per tutti i 15 articoli + aggiornare ArticleRenderer</name>
  <files>content/testi-sito-attuale/blog/*.md, src/components/sections/ArticleRenderer.tsx</files>
  <action>
1. Leggere tutti i 15 file .md in `content/testi-sito-attuale/blog/` (escludere index.md).

2. Per ciascun articolo, analizzare il contenuto e inserire 2-5 heading H2 (`## Titolo`) nei punti di transizione tematica naturale:
   - Headlines in italiano, coerenti con il tono narrativo/personale dell'autrice
   - NON aggiungere H3 (solo H2 per mantenere semplicita')
   - NON modificare il testo esistente — solo inserire righe `## Heading` dove il discorso cambia tema
   - Per il-barbone.md e va-dove-ti-porta-il-cane.md che gia' hanno H2: verificare e integrare se servono altre
   - Posizionare le headline PRIMA del paragrafo che introducono (riga vuota sopra e sotto)

3. Aggiornare `src/components/sections/ArticleRenderer.tsx`:
   - Creare funzione `slugify(text: string): string` che converte testo in id URL-safe:
     - Lowercase, trim, replace spazi con `-`, rimuove caratteri non alfanumerici (tranne `-`)
     - Gestisce caratteri accentati italiani (e', a', etc.)
   - Modificare i custom components `h2` e `h3` per aggiungere attributo `id`:
     ```tsx
     h2: ({ children }) => {
       const text = typeof children === 'string' ? children : String(children)
       return <h2 id={slugify(text)} className="article-h2">{children}</h2>
     },
     h3: ({ children }) => {
       const text = typeof children === 'string' ? children : String(children)
       return <h3 id={slugify(text)} className="article-h3">{children}</h3>
     },
     ```
   - Nota: `children` da react-markdown puo' essere array di nodi React — usare una helper che estrae testo ricorsivamente:
     ```tsx
     function extractText(node: React.ReactNode): string {
       if (typeof node === 'string') return node
       if (Array.isArray(node)) return node.map(extractText).join('')
       if (node && typeof node === 'object' && 'props' in node) return extractText(node.props.children)
       return ''
     }
     ```

4. Preparare un riepilogo delle headlines proposte per il checkpoint (Task 2).
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Tutti i 15 .md hanno almeno 2 heading H2, ArticleRenderer genera id su h2/h3</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Approvazione headlines articoli</name>
  <action>
Headlines H2 inserite in tutti i 15 articoli blog. Ogni articolo ha 2-5 heading che dividono il testo in sezioni tematiche. ArticleRenderer genera id per anchor links.

Verificare:
1. Avviare `npm run dev` e visitare http://localhost:5173/blog
2. Aprire 3-4 articoli a caso e verificare:
   - Le headline H2 appaiono come titoli di sezione nel testo
   - Sono posizionate in punti di transizione tematica naturale
   - Il tono e' coerente con lo stile narrativo dell'autrice
   - Non interrompono il flusso della lettura
3. Ispezionare un h2 nel DOM: deve avere un attributo `id` (es. `id="il-primo-incontro"`)
4. Se alcune headline non convincono, indicare quale articolo e quale headline cambiare
  </action>
  <verify>Utente digita "approvato" oppure indica le headline da modificare</verify>
  <done>Headlines approvate dall'utente, pronte per TOC sidebar</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- `grep -c "^##" content/testi-sito-attuale/blog/*.md` mostra almeno 2 per ogni file (escluso index.md)
- Ispezionare DOM: h2 hanno attributo id generato da slugify
</verification>

<success_criteria>
15 articoli blog hanno headlines H2 approvate dall'utente. ArticleRenderer genera id attributi su h2/h3 per anchor linking dalla sidebar TOC.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-14-SUMMARY.md`
</output>
