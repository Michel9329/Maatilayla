---
phase: 04-pagine-interne
plan: 10
type: execute
wave: 2
depends_on: ["04-08"]
files_modified:
  - src/pages/BlogArticle.tsx
  - src/components/sections/ArticleRenderer.tsx
  - src/App.tsx
  - src/index.css
autonomous: true
requirements: [BLOG-ARTICLE]

must_haves:
  truths:
    - "L'utente puo navigare a /blog/:slug e leggere l'articolo completo"
    - "Il markdown e renderizzato con tipografia leggibile (Playfair heading, Poppins body)"
    - "Articoli con immagine mostrano la copertina, quelli senza hanno header compatto"
    - "Slug inesistente fa redirect a /blog"
    - "Ogni articolo ha SEO completo (Helmet, OG article, JSON-LD BlogPosting)"
    - "Link 'Tutti gli articoli' riporta a /blog"
    - "Navigazione prev/next in fondo all'articolo"
  artifacts:
    - path: "src/pages/BlogArticle.tsx"
      provides: "Pagina singolo articolo con header, copertina, markdown body, SEO, prev/next"
      min_lines: 80
    - path: "src/components/sections/ArticleRenderer.tsx"
      provides: "Wrapper react-markdown con custom components e stili tipografici"
      min_lines: 30
    - path: "src/App.tsx"
      provides: "Route /blog/:slug aggiunta"
      contains: "blog/:slug"
  key_links:
    - from: "src/pages/BlogArticle.tsx"
      to: "src/data/blogArticles.ts"
      via: "getArticleBySlug"
      pattern: "getArticleBySlug"
    - from: "src/App.tsx"
      to: "src/pages/BlogArticle.tsx"
      via: "lazy import + Route"
      pattern: "BlogArticle"
    - from: "src/components/sections/ArticleRenderer.tsx"
      to: "react-markdown"
      via: "import Markdown"
      pattern: "from.*react-markdown"
---

<objective>
Costruire la pagina singolo articolo /blog/:slug con rendering markdown, tipografia, SEO e navigazione.

Purpose: La pagina articolo e dove l'utente legge il contenuto — deve essere leggibile, ben tipografata, con SEO completo per indicizzazione.
Output: BlogArticle.tsx, ArticleRenderer.tsx, route in App.tsx, CSS tipografico.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pagine-interne/04-BLOG-RESEARCH.md
@.planning/phases/04-pagine-interne/04-08-SUMMARY.md

@src/data/blogArticles.ts
@src/App.tsx
@src/index.css
</context>

<interfaces>
<!-- Da 04-08 (data layer): -->
```typescript
export interface BlogArticle {
  slug: string
  title: string
  date: string
  category: string
  excerpt: string
  image?: string
  imageAlt?: string
  imagePosition?: string
  content: string
  readingTime: number
}

export const blogArticlesSorted: BlogArticle[]
export function getArticleBySlug(slug: string): BlogArticle | undefined
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Creare ArticleRenderer e pagina BlogArticle con route</name>
  <files>src/components/sections/ArticleRenderer.tsx, src/pages/BlogArticle.tsx, src/App.tsx</files>
  <action>
1. Creare `src/components/sections/ArticleRenderer.tsx`:
   - Import `Markdown` da `react-markdown`
   - Props: `content: string`
   - Preprocessare il contenuto: rimuovere la prima riga `# Titolo` e la seconda riga `*data*` dal markdown (gia mostrati nell'header della pagina — evitare duplicazione). Usare replace regex: rimuovere `^#\s+.+\n` e `^\*.+\*\n`
   - Custom components per react-markdown:
     - `h2` → `<h2 className="article-h2">`
     - `h3` → `<h3 className="article-h3">`
     - `p` → `<p className="article-p">`
     - `strong` → `<strong className="article-strong">`
     - `em` → `<em className="article-em">`
     - `a` → `<a className="article-link" target="_blank" rel="noopener noreferrer">`
     - `ul` → `<ul className="article-ul">`
     - `li` → `<li className="article-li">`
   - Wrapper `<div className="article-body">`

2. Creare `src/pages/BlogArticle.tsx`:
   - Import `useParams`, `Navigate`, `Link` da `react-router`
   - Import `Helmet` da `react-helmet-async`
   - Import `getArticleBySlug`, `blogArticlesSorted` da `@/data/blogArticles`
   - Import `ArticleRenderer`

   - Lookup articolo via `getArticleBySlug(slug)`. Se non trovato → `<Navigate to="/blog" replace />`

   - Calcolare prev/next: trovare indice dell'articolo corrente in `blogArticlesSorted`, prev = indice+1 (piu vecchio), next = indice-1 (piu recente). Null se ai bordi.

   - Layout pagina:
     - `<article className="ba-article">` — max-width 720px, margin auto
     - Header:
       - Link "Tutti gli articoli" con freccia (`<Link to="/blog" className="ba-back">`)
       - Meta row: categoria pill + data formattata (`toLocaleDateString('it-IT', {day:'numeric', month:'long', year:'numeric'})`) + tempo lettura
       - `<h1 className="ba-title">` — Playfair Display, clamp(1.6rem, 3.5vw, 2.4rem)
     - Immagine copertina (se presente):
       - `<figure className="ba-cover">` con `<img>` — `decoding="async"` + `fetchPriority="high"` (no lazy — above the fold)
       - Border-radius var(--radius-card), max-width 100%
     - `<ArticleRenderer content={article.content} />`
     - Navigazione prev/next:
       - `<nav className="ba-nav">` con due link (prev e next)
       - Prev: freccia sinistra + titolo articolo precedente
       - Next: titolo articolo successivo + freccia destra
       - Solo uno dei due se non c'e l'altro

   - SEO Helmet completo:
     - `<title>{article.title} — Blog Maatilayla</title>`
     - meta description = article.excerpt
     - canonical link
     - OG: title, description, type "article", url, image (se presente), locale it_IT, site_name
     - article:published_time
     - JSON-LD BlogPosting con headline, description, datePublished, author (Layla Zarfati), publisher (Maatilayla), mainEntityOfPage, image

3. Aggiornare `src/App.tsx`:
   - Aggiungere lazy import: `const BlogArticle = lazy(() => import('@/pages/BlogArticle'))`
   - Aggiungere route dentro le Routes, dopo la route `/blog`:
     `<Route path="/blog/:slug" element={<BlogArticle />} />`
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pagina /blog/:slug renderizza articolo markdown, SEO completo, prev/next navigazione, redirect per slug invalido, route registrata in App.tsx</done>
</task>

<task type="auto">
  <name>Task 2: CSS per articolo singolo (prefisso ba- e article-)</name>
  <files>src/index.css</files>
  <action>
Aggiungere in `src/index.css` stili per articolo singolo:

**Layout articolo (ba-):**
1. `.ba-article` — max-width 720px, margin 0 auto, padding 2rem 1.5rem 4rem, color var(--color-text)
2. `.ba-back` — display inline-flex, align-items center, gap 0.5rem, font-size 0.9rem, color var(--color-text-muted), text-decoration none, margin-bottom 2rem, hover color primary, transition color 0.2s
3. `.ba-header` — margin-bottom 2rem
4. `.ba-meta` — display flex, flex-wrap wrap, gap 0.75rem, align-items center, margin-bottom 1rem
5. `.ba-category` — font-size 0.8rem, font-weight 600, color var(--color-primary), background var(--color-primary-pale), padding 0.25rem 0.75rem, border-radius 1rem
6. `.ba-date` — font-size 0.85rem, color var(--color-text-muted)
7. `.ba-readtime` — font-size 0.85rem, color var(--color-text-muted)
8. `.ba-title` — font-family var(--font-heading), font-size clamp(1.6rem, 3.5vw, 2.4rem), font-weight 700, line-height 1.2, color var(--color-text)
9. `.ba-cover` — margin 0 0 2rem, border-radius var(--radius-card), overflow hidden
10. `.ba-cover img` — width 100%, height auto, display block

**Tipografia articolo (article-):**
11. `.article-body` — font-family var(--font-body), font-size clamp(1rem, 1.1vw, 1.125rem), line-height 1.75, color var(--color-text)
12. `.article-h2` — font-family var(--font-heading), font-size clamp(1.3rem, 2vw, 1.6rem), font-weight 700, margin 2.5rem 0 1rem, line-height 1.3, color var(--color-text)
13. `.article-h3` — font-family var(--font-heading), font-size clamp(1.1rem, 1.5vw, 1.3rem), font-weight 600, margin 2rem 0 0.75rem, line-height 1.35
14. `.article-p` — margin-bottom 1.25rem
15. `.article-strong` — font-weight 600, color var(--color-text)
16. `.article-em` — font-style italic, color var(--color-text-muted)
17. `.article-link` — color var(--color-primary), text-decoration underline, text-underline-offset 3px, transition color 0.2s, hover color primary-light
18. `.article-ul` — margin 1rem 0, padding-left 1.5rem, list-style disc
19. `.article-li` — margin-bottom 0.5rem

**Navigazione prev/next (ba-nav):**
20. `.ba-nav` — display flex, justify-content space-between, gap 1.5rem, margin-top 3rem, padding-top 2rem, border-top 1px solid rgba(0,0,0,0.08)
21. `.ba-nav a` — display flex, flex-direction column, gap 0.25rem, text-decoration none, max-width 45%, transition color 0.2s
22. `.ba-nav a:hover` — color primary
23. `.ba-nav-label` — font-size 0.8rem, color var(--color-text-muted), font-weight 500
24. `.ba-nav-title` — font-size 0.95rem, font-weight 600, color var(--color-text), line-clamp 2

**Responsive:**
25. `@media (max-width: 767px)` — `.ba-article` padding 1.5rem 1rem 3rem, `.ba-nav` flex-direction column gap 1.5rem, `.ba-nav a` max-width 100%

**Reduced motion:** nessuna animazione da gestire (pagina statica di lettura)
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CSS tipografico completo per articolo, leggibile su tutti i breakpoint, navigazione prev/next stilizzata, coerente con design system</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- Navigare a /blog/controlli-si-grazie mostra articolo completo con tipografia
- Navigare a /blog/slug-inesistente fa redirect a /blog
- Prev/next navigation funziona tra articoli
- Immagine copertina visibile su articoli che ce l'hanno
- Articoli senza immagine hanno header compatto (no spazio vuoto)
- SEO: title, description, OG, JSON-LD presenti nel source
- Responsive: leggibile su mobile (max-width 720px, padding adeguato)
</verification>

<success_criteria>
- Pagina articolo leggibile con tipografia curata
- Rendering markdown corretto (grassetto, corsivo, heading, link, liste)
- SEO completo per ogni articolo (Helmet + JSON-LD BlogPosting)
- Navigazione prev/next funzionale
- Route /blog/:slug registrata e lazy-loaded
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-10-SUMMARY.md`
</output>
