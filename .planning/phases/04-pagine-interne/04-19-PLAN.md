---
phase: 04-pagine-interne
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ContactInfoSection.tsx
  - src/components/sections/ContactMapSection.tsx
  - src/index.css
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03

must_haves:
  truths:
    - "Utente vede card info contatto (orari, telefono, email, indirizzo) sopra il form"
    - "Utente vede mappa Mapbox interattiva con stile pastello/caldo"
    - "Mappa mostra marker su Bassano Romano (VT)"
    - "Callout evidenziato 'visite solo su appuntamento' ben visibile"
    - "Mappa si carica lazy (solo quando visibile)"
  artifacts:
    - path: "src/components/sections/ContactInfoSection.tsx"
      provides: "Card info contatti + callout visite"
      min_lines: 60
    - path: "src/components/sections/ContactMapSection.tsx"
      provides: "Mappa Mapbox con stile custom pastello"
      min_lines: 40
    - path: "src/index.css"
      provides: "Classi CSS ci-* per info e cm-* per mappa"
      contains: ".ci-section"
  key_links:
    - from: "src/components/sections/ContactMapSection.tsx"
      to: "mapbox-gl"
      via: "import mapboxgl from 'mapbox-gl'"
      pattern: "import mapboxgl"
---

<objective>
Creare la sezione info contatti con card dati, callout visite su appuntamento, e mappa Mapbox con stile pastello.

Purpose: Fornire informazioni di contatto visivamente ricche sopra il form ContactSection, con mappa navigabile stilizzata nei colori del sito.

Output: ContactInfoSection.tsx, ContactMapSection.tsx, CSS ci-*/cm-* in index.css
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pagine-interne/04-CONTATTI-CONTEXT.md
@content/testi-sito-attuale/contatti.md
@src/components/sections/ContactSection.tsx
@src/index.css
@src/pages/Contatti.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installare mapbox-gl e configurare env</name>
  <files>package.json, .env.example</files>
  <action>
1. Installare: `npm install mapbox-gl`
2. Installare tipi: `npm install -D @types/mapbox-gl`
3. Aggiungere in `.env.example`:
   ```
   # Mapbox (mappa contatti)
   VITE_MAPBOX_TOKEN=your_mapbox_public_token
   ```
4. Verificare che il token verra' letto come `import.meta.env.VITE_MAPBOX_TOKEN`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>mapbox-gl installato, .env.example aggiornato con VITE_MAPBOX_TOKEN.</done>
</task>

<task type="auto">
  <name>Task 2: ContactInfoSection — card info + callout visite</name>
  <files>src/components/sections/ContactInfoSection.tsx, src/index.css</files>
  <action>
**Componente `ContactInfoSection.tsx`:**

1. **Layout**: sezione con griglia 2x2 card info + callout sotto
2. **Card info** (4 card, icone Lucide):
   - MapPin: "Indirizzo" — Strada Vicinale Pianamola 6, 01030 Bassano Romano (VT)
   - Phone: "Telefono" — 07 61 179 0344 (fisso) + 338 761 7628 (cell/WhatsApp)
   - Mail: "Email" — maatilayla.org@gmail.com
   - Clock: "Orari" — Lun-Sab: 9:00-13:00 | 15:30-19:00 (solo chiamate)
3. **Ogni card**: icona in cerchio primary-pale, titolo bold, contenuto con link (tel:, mailto:, maps)
4. **Callout visite** (sotto le card):
   - Stile callout evidenziato (bordo sinistro primary, bg primary-pale/20%, icona ShieldAlert o CalendarClock)
   - Testo: "La sede dell'allevamento e' anche la nostra casa. Per tutelare la privacy della famiglia, riceviamo esclusivamente su appuntamento, dopo un primo contatto telefonico o via email."
   - Nota secondaria (font piu' piccolo, muted): "Il cellulare potrebbe risultare non raggiungibile nelle zone decentrate. In tal caso, contattateci via WhatsApp o email."
5. **IntersectionObserver**: entrance ci-entered, check reduced-motion

**CSS in index.css** — classi `ci-*`:
- `.ci-section`: padding 4rem 1.5rem, max-width 1100px, margin auto, opacity 0 translateY(20px), transition 0.6s
- `.ci-section.ci-entered`: opacity 1, translateY(0)
- `.ci-grid`: display grid, grid-template-columns repeat(2, 1fr), gap 1.5rem
- `.ci-card`: bg cream, border-radius card, padding 1.75rem, display flex, gap 1rem, align-items flex-start
- `.ci-icon`: width 48px, height 48px, border-radius 50%, bg primary-pale, display flex, align-items center, justify-content center, flex-shrink 0, color primary
- `.ci-card-title`: font-weight 600, font-size 0.95rem, color text, margin-bottom 0.25rem
- `.ci-card-content`: font-size 0.9rem, color text-muted, line-height 1.6
- `.ci-card-content a`: color primary, text-decoration none, transition color 0.2s
- `.ci-card-content a:hover`: color primary-light
- `.ci-card-content a:focus-visible`: outline 2px primary, offset 2px
- `.ci-note`: font-size 0.8rem, color text-muted, font-style italic
- `.ci-callout`: margin-top 2rem, padding 1.5rem 1.75rem, border-left 4px solid primary, bg rgba(var(--color-primary-pale-rgb, 250,224,208), 0.2), border-radius 0 card card 0, display flex, gap 1rem, align-items flex-start
- `.ci-callout-icon`: color primary, flex-shrink 0, margin-top 2px
- `.ci-callout-text`: font-size 0.95rem, color text, line-height 1.6
- `.ci-callout-note`: font-size 0.82rem, color text-muted, margin-top 0.5rem
- Media max 768px: ci-grid grid-template-columns 1fr (stack singola colonna)
- Media 1024px: ci-section padding 5rem 2.5rem
- Reduced motion: ci-section opacity 1 transform none transition none
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ContactInfoSection con 4 card info + callout visite. CSS ci-* responsive. Zero errori TS.</done>
</task>

<task type="auto">
  <name>Task 3: ContactMapSection — mappa Mapbox pastello</name>
  <files>src/components/sections/ContactMapSection.tsx, src/index.css</files>
  <action>
**Componente `ContactMapSection.tsx`:**

1. **Lazy load**: IntersectionObserver — carica mappa solo quando il container diventa visibile (threshold 0, rootMargin '200px')
2. **Mapbox GL JS**:
   - Container ref con id univoco
   - Token: `import.meta.env.VITE_MAPBOX_TOKEN`
   - Stile: usare `mapbox://styles/mapbox/light-v11` come base e applicare paint overrides per colori pastello, OPPURE se non c'e' token, mostrare un placeholder statico con link a Google Maps
   - Centro: [12.1958, 42.2317] (Bassano Romano coordinate approssimative)
   - Zoom: 13
   - Marker: su coordinate allevamento, colore primary (#C8614A)
   - Popup sul marker: "Allevamento Maatilayla" + indirizzo + link "Apri in Google Maps"
3. **Fallback senza token**: se VITE_MAPBOX_TOKEN non e' definito, mostrare un div placeholder con sfondo cream, icona MapPin grande, indirizzo e link Google Maps. Non crashare.
4. **Cleanup**: map.remove() su unmount
5. **CSS mapbox-gl**: importare `mapbox-gl/dist/mapbox-gl.css`
6. **Stile custom mappa** (paint overrides dopo load):
   - Acqua: #E8DDD0 (beige chiaro)
   - Terra/parchi: #F0E8DC
   - Strade principali: #D4B896 (accent)
   - Strade secondarie: #EDE5D8
   - Edifici: #E0D4C4
   - Testo labels: #6B5040 (text-muted)
   Applicare via `map.on('load', () => { map.setPaintProperty(...) })` per ogni layer

**CSS in index.css** — classi `cm-*`:
- `.cm-section`: padding 0 1.5rem 4rem, max-width 1100px, margin auto
- `.cm-map-container`: width 100%, height 400px, border-radius card, overflow hidden, box-shadow 0 4px 16px rgba(0,0,0,0.08)
- `.cm-placeholder`: width 100%, height 400px, border-radius card, bg cream, display flex, flex-direction column, align-items center, justify-content center, gap 1rem, color text-muted
- `.cm-placeholder-icon`: color primary, opacity 0.6
- `.cm-placeholder-link`: color primary, font-weight 500, text-decoration none
- `.cm-placeholder-link:hover`: color primary-light
- Media 768px: cm-map-container height 450px, cm-section padding 0 2rem 5rem
- Media 1024px: cm-map-container height 500px
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ContactMapSection con mappa Mapbox lazy-loaded, stile pastello, marker custom, fallback senza token. CSS cm-*. Zero errori TS.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa senza errori
- `npm run build` completa senza errori
- Mappa non crasha senza VITE_MAPBOX_TOKEN (fallback placeholder)
- Card info hanno link funzionanti (tel, mailto, maps)
- Callout visite ben visibile
</verification>

<success_criteria>
- ContactInfoSection con 4 card + callout visite
- ContactMapSection con mappa Mapbox pastello o fallback
- Accessibilita': link con aria-label, focus-visible, reduced-motion
- Build pulita
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-19-SUMMARY.md`
</output>
</content>
</invoke>