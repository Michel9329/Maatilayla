---
phase: 04-pagine-interne
plan: 09
type: execute
wave: 2
depends_on: ["04-08"]
files_modified:
  - src/pages/Blog.tsx
  - src/components/sections/BlogGrid.tsx
  - src/index.css
autonomous: true
requirements: [BLOG-LISTING]

must_haves:
  truths:
    - "L'utente vede tutti i 15 articoli in una griglia responsive su /blog"
    - "Ogni card mostra immagine (o fallback), categoria, data, titolo, excerpt, tempo di lettura"
    - "L'utente puo filtrare gli articoli per categoria con pill cliccabili"
    - "Click su una card porta a /blog/:slug"
    - "La pagina ha SEO completo (Helmet, OG, JSON-LD Blog schema)"
  artifacts:
    - path: "src/components/sections/BlogGrid.tsx"
      provides: "Griglia responsive card articoli con filtro categoria"
      min_lines: 60
    - path: "src/pages/Blog.tsx"
      provides: "Pagina /blog completa con hero + griglia + SEO"
      min_lines: 40
    - path: "src/index.css"
      provides: "Stili bg- per griglia blog e card"
      contains: ".bg-grid"
  key_links:
    - from: "src/pages/Blog.tsx"
      to: "src/data/blogArticles.ts"
      via: "import blogArticlesSorted, blogCategories"
      pattern: "import.*from.*blogArticles"
    - from: "src/components/sections/BlogGrid.tsx"
      to: "/blog/:slug"
      via: "Link to={`/blog/${article.slug}`}"
      pattern: "to=.*blog.*slug"
---

<objective>
Costruire la pagina listing /blog con griglia responsive di tutti gli articoli e filtro per categoria.

Purpose: La pagina /blog e il punto di accesso principale al blog — deve mostrare tutti i 15 articoli in modo chiaro e navigabile.
Output: Blog.tsx completo con BlogGrid, filtri categoria, SEO, CSS responsive.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pagine-interne/04-BLOG-RESEARCH.md
@.planning/phases/04-pagine-interne/04-08-SUMMARY.md

@src/data/blogArticles.ts
@src/pages/Blog.tsx
@src/index.css
</context>

<interfaces>
<!-- Da 04-08 (data layer): -->
```typescript
export interface BlogArticle {
  slug: string
  title: string
  date: string
  category: string
  excerpt: string
  image?: string
  imageAlt?: string
  imagePosition?: string
  content: string
  readingTime: number
}

export const blogArticlesSorted: BlogArticle[]
export const blogCategories: string[]
export function getArticleBySlug(slug: string): BlogArticle | undefined
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Creare BlogGrid e espandere Blog.tsx con griglia + filtri + SEO</name>
  <files>src/components/sections/BlogGrid.tsx, src/pages/Blog.tsx</files>
  <action>
1. Creare `src/components/sections/BlogGrid.tsx`:
   - Props: `articles: BlogArticle[]`, `categories: string[]`
   - State: `activeCategory: string | null` (null = tutti)
   - Filtro: riga di pill/bottoni per categoria ("Tutti" + ogni categoria). Pill attiva ha sfondo primary, le altre outline.
   - Griglia: CSS grid 3 colonne desktop (1024px+), 2 tablet (768px+), 1 mobile
   - Card: ogni articolo come `<Link to={/blog/${article.slug}}>` con:
     - Blocco immagine `.bg-card-img` (16/10 aspect-ratio, object-fit cover). Se `article.image` manca, mostrare sfondo cream con gradient subtle (no immagine rotta).
     - Meta row: categoria (pill primary-pale) + data formattata in italiano
     - Titolo h3 con line-clamp 2
     - Excerpt con line-clamp 3
     - Footer: tempo di lettura + "Leggi" con freccia
   - IntersectionObserver per entrance animation (fade-up, come pattern progetto)
   - `contain: layout style paint` sui container card con immagini
   - `loading="lazy" decoding="async"` su tutte le immagini
   - Accessibilita: `aria-label` sulla section, link con aria-label articolo, focus-visible sulle card

2. Espandere `src/pages/Blog.tsx`:
   - Mantenere HeroSection esistente
   - Aggiungere import di `blogArticlesSorted` e `blogCategories` da `@/data/blogArticles`
   - Aggiungere `<BlogGrid articles={blogArticlesSorted} categories={blogCategories} />` dopo l'hero
   - Aggiungere JSON-LD schema `Blog` + `BlogPosting[]` nel Helmet (come da RESEARCH)
   - Aggiungere canonical link
   - Aggiungere InstagramFeedSection e ContactSection in fondo (pattern pagine interne)
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pagina /blog mostra tutti i 15 articoli in griglia responsive, filtri categoria funzionano, SEO completo</done>
</task>

<task type="auto">
  <name>Task 2: CSS per griglia blog (prefisso bg-)</name>
  <files>src/index.css</files>
  <action>
Aggiungere in fondo a `src/index.css` (prima dei media query finali se presenti) tutti gli stili con prefisso `bg-`:

1. `.bg-section` — padding-block, sfondo warm-white
2. `.bg-container` — max-width 1200px, margin auto, padding inline
3. `.bg-filters` — riga pill, flex wrap, gap 0.5rem, margin-bottom 2rem
4. `.bg-filter-pill` — padding 0.5rem 1.25rem, border-radius var(--radius-btn), border 1px solid, font-size 0.85rem, font-weight 500, cursor pointer, transition background/color 0.2s
5. `.bg-filter-pill.active` — sfondo primary, color white, border primary
6. `.bg-filter-pill:not(.active)` — sfondo transparent, color text-muted, border text-muted/30%, hover sfondo primary-pale
7. `.bg-grid` — CSS grid, 3 colonne desktop (minmax(300px, 1fr)), gap 2rem
8. `.bg-card` — display flex column, border-radius var(--radius-card), overflow hidden, background cream, text-decoration none, color inherit, transition box-shadow 0.25s, focus-visible outline
9. `.bg-card:hover` — box-shadow 0 8px 32px rgba(0,0,0,0.08)
10. `.bg-card-img` — aspect-ratio 16/10, overflow hidden, background primary-pale (fallback)
11. `.bg-card-img img` — width 100%, height 100%, object-fit cover, transition transform 0.3s
12. `.bg-card:hover .bg-card-img img` — transform scale(1.03)
13. `.bg-card-body` — padding 1.25rem, flex 1, display flex, flex-direction column
14. `.bg-card-meta` — flex, gap 0.75rem, font-size 0.8rem, margin-bottom 0.5rem
15. `.bg-card-category` — color primary, font-weight 600
16. `.bg-card-date` — color text-muted
17. `.bg-card-title` — font-family var(--font-heading), font-size clamp(1rem, 1.2vw, 1.15rem), font-weight 700, line-height 1.3, margin-bottom 0.5rem, display -webkit-box, -webkit-line-clamp 2, overflow hidden
18. `.bg-card-excerpt` — font-size 0.9rem, color text-muted, line-height 1.6, display -webkit-box, -webkit-line-clamp 3, overflow hidden, flex 1
19. `.bg-card-footer` — display flex, justify-content space-between, align-items center, margin-top 1rem, padding-top 0.75rem, border-top 1px solid rgba(0,0,0,0.06)
20. `.bg-card-readtime` — font-size 0.8rem, color text-muted
21. `.bg-card-link` — font-size 0.85rem, font-weight 600, color primary

22. Entrance animation (pattern progetto):
    - `.bg-section .bg-card` — opacity 0, translate 0 24px, transition opacity 0.5s, translate 0.5s
    - `.bg-section.bg-entered .bg-card` — opacity 1, translate 0 0
    - Stagger con transition-delay su nth-child (max 6 visibili, delay 0.06s * n)

23. Responsive:
    - `@media (max-width: 1023px)` — `.bg-grid` 2 colonne
    - `@media (max-width: 767px)` — `.bg-grid` 1 colonna
    - Padding container ridotto su mobile

24. `@media (prefers-reduced-motion: reduce)` — rimuovere translate e transition sulle card

25. Stile `.bg-no-image` per card senza immagine — sfondo gradient cream→primary-pale nel blocco immagine, con icona o pattern subtle (a discrezione)
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Stili bg- completi per griglia blog, responsive 3/2/1 colonne, entrance animation, hover, reduced-motion, fallback immagini</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- Pagina /blog mostra 15 card in griglia
- Filtri categoria funzionano (click filtra, "Tutti" resetta)
- Card senza immagine hanno fallback visivo (no broken image)
- Responsive: 3 colonne desktop, 2 tablet, 1 mobile
- Helmet ha title, description, OG, JSON-LD
</verification>

<success_criteria>
- Pagina /blog completa e navigabile con tutti i 15 articoli
- Filtri categoria funzionali
- SEO completo (Helmet + JSON-LD Blog)
- Layout responsive 3/2/1 colonne
- Entrance animation e hover states
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-09-SUMMARY.md`
</output>
