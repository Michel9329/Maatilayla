---
phase: 04-pagine-interne
plan: 07
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - src/components/sections/ChiSiamoCta.tsx
  - src/pages/ChiSiamo.tsx
  - src/index.css
autonomous: false
requirements: [CS-01, CS-02, CS-03, CS-04, CS-05, CS-06, CS-07]

must_haves:
  truths:
    - "La pagina /chi-siamo mostra 8 sezioni nell'ordine corretto: Hero → Storia → Credenziali → Valori → Struttura → Timeline → Mappa → CTA"
    - "La CTA finale ha foto background, testo caldo e bottone che linka a /contatti"
    - "Il deep-link /chi-siamo#struttura funziona (dalla Hero CTA 'Struttura')"
    - "Il deep-link /chi-siamo#storia funziona (dalla Hero CTA 'Storia')"
    - "Il build di produzione non ha errori TypeScript o ESLint"
    - "La pagina è responsive e visivamente coerente con la homepage"
  artifacts:
    - path: "src/components/sections/ChiSiamoCta.tsx"
      provides: "CTA finale con foto background e link a /contatti"
      exports: ["default ChiSiamoCta"]
    - path: "src/pages/ChiSiamo.tsx"
      provides: "Pagina Chi Siamo completa con tutte le 8 sezioni"
      contains: "StoriaLaylaSection, CredenzialiSection, ValoriSection, StrutturaDSection, TimelineSection, WorldMapSection, ChiSiamoCta"
    - path: "src/index.css"
      provides: "CSS prefisso cs- per ChiSiamoCta"
      contains: ".cs-section"
  key_links:
    - from: "src/pages/ChiSiamo.tsx"
      to: "src/components/sections/StoriaLaylaSection.tsx"
      via: "import e render dopo HeroSection"
      pattern: "StoriaLaylaSection"
    - from: "src/pages/ChiSiamo.tsx"
      to: "src/components/sections/ChiSiamoCta.tsx"
      via: "import e render come ultima sezione"
      pattern: "ChiSiamoCta"
    - from: "src/components/sections/ChiSiamoCta.tsx"
      to: "/contatti"
      via: "Link React Router"
      pattern: "to=\"/contatti\""
---

<objective>
Completare la pagina Chi Siamo: creare ChiSiamoCta (sezione finale), assemblare tutte le sezioni in ChiSiamo.tsx, verificare checkpoint visivo.

Purpose: Piano finale della fase Chi Siamo. Collega tutti i componenti costruiti nelle waves precedenti in una pagina funzionante e completa, con la CTA di chiusura.
Output: /chi-siamo completa con 8 sezioni, build production pulita, checkpoint umano per verifica visiva.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md

@src/pages/ChiSiamo.tsx
@src/components/sections/FaqCtaSection.tsx

<interfaces>
<!-- Pattern CTA finale da FaqCtaSection:
  - Foto background con parallax GSAP
  - Contenuto centrato sovrapposto (badge, titolo word-by-word, body, CTA button)
  - Sfondo warm-white per alternanza con cream della WorldMapSection
  - Usare una foto cuccioli disponibile: maatilayla-cuccioli-barboncino-toy-cta.webp
  - Bottone → /contatti
-->

<!-- Ordine sezioni in ChiSiamo.tsx:
  1. HeroSection (già presente — NON TOCCARE)
  2. StoriaLaylaSection  (Plan 01)
  3. CredenzialiSection  (Plan 02)
  4. ValoriSection       (Plan 03)
  5. StrutturaDSection   (Plan 04) — ha id="struttura"
  6. TimelineSection     (Plan 05)
  7. WorldMapSection     (Plan 06)
  8. ChiSiamoCta         (questo plan)
-->

<!-- Deep-links dalla Hero:
  - id="storia" deve essere su StoriaLaylaSection (aggiungere al section element)
  - id="struttura" è già su StrutturaDSection
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare ChiSiamoCta + CSS cs- in index.css</name>
  <files>src/components/sections/ChiSiamoCta.tsx, src/index.css</files>
  <action>
**Parte A — Componente ChiSiamoCta.tsx:**

Ispirarsi a FaqCtaSection ma con tono caldo orientato ai contatti. Usare pattern IntersectionObserver + GSAP parallax identico.

```tsx
import { useEffect, useRef, useMemo } from 'react'
import { Link } from 'react-router'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

const TITLE_TEXT = 'Inizia la tua storia con noi.'
const ACCENT_WORDS = new Set(['storia'])

export default function ChiSiamoCta() {
  const sectionRef = useRef<HTMLElement>(null)
  const imgRef = useRef<HTMLImageElement>(null)
  const badgeRef = useRef<HTMLSpanElement>(null)
  const titleRef = useRef<HTMLHeadingElement>(null)
  const bodyRef = useRef<HTMLParagraphElement>(null)
  const ctaRef = useRef<HTMLDivElement>(null)

  const words = useMemo(() => TITLE_TEXT.split(' '), [])

  useEffect(() => {
    const section = sectionRef.current
    const img = imgRef.current
    if (!section || !img) return

    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches

    // Parallax foto
    const parallaxTl = gsap.timeline({
      scrollTrigger: {
        trigger: section,
        start: 'top bottom',
        end: 'bottom top',
        scrub: 0.6,
      },
    })
    parallaxTl.fromTo(img, { yPercent: -6 }, { yPercent: 6, ease: 'none' })

    if (prefersReduced || !window.matchMedia('(min-width: 768px)').matches) {
      // Mobile: IntersectionObserver CSS
      const observer = new IntersectionObserver(
        ([entry]) => {
          requestAnimationFrame(() => {
            if (entry.isIntersecting) section.classList.add('cs-entered')
            else section.classList.remove('cs-entered')
          })
        },
        { threshold: 0, rootMargin: '-10% 0px' },
      )
      observer.observe(section)
      return () => { parallaxTl.kill(); observer.disconnect() }
    }

    // Desktop: word-by-word GSAP (pattern FaqCtaSection)
    const wordEls = titleRef.current?.querySelectorAll<HTMLElement>('.cs-word') ?? []
    gsap.set(badgeRef.current, { opacity: 0, y: 24 })
    gsap.set(wordEls, { opacity: 0, y: 16 })
    gsap.set(bodyRef.current, { opacity: 0, y: 14 })
    gsap.set(ctaRef.current, { opacity: 0, y: 10, scale: 0.97 })

    const tl = gsap.timeline({ paused: true })
    tl.to(badgeRef.current, { opacity: 1, y: 0, duration: 0.5, ease: 'power2.out' })
      .to(wordEls, { opacity: 1, y: 0, duration: 0.4, stagger: 0.07, ease: 'power2.out' }, '-=0.2')
      .to(bodyRef.current, { opacity: 1, y: 0, duration: 0.5, ease: 'power2.out' }, '-=0.15')
      .to(ctaRef.current, { opacity: 1, y: 0, scale: 1, duration: 0.45, ease: 'power2.out' }, '-=0.2')

    const st = ScrollTrigger.create({
      trigger: section,
      start: 'top 80%',
      end: 'bottom 20%',
      onEnter: () => tl.restart(),
      onEnterBack: () => tl.restart(),
      onLeave: () => tl.pause(0),
      onLeaveBack: () => tl.pause(0),
    })

    return () => { parallaxTl.kill(); tl.kill(); st.kill() }
  }, [])

  return (
    <section className="cs-section" ref={sectionRef} aria-label="Contattaci">
      <img
        ref={imgRef}
        src="/content/images/maatilayla-cuccioli-barboncino-toy-cta.webp"
        alt="Cuccioli di barboncino toy dell'allevamento Maatilayla pronti per la nuova famiglia"
        loading="lazy"
        decoding="async"
        className="cs-bg"
      />
      <div className="cs-content">
        <span className="cs-badge" ref={badgeRef}>Contattaci</span>
        <h2 className="cs-title" ref={titleRef}>
          {words.map((word, i) => (
            <span
              key={i}
              className={`cs-word${ACCENT_WORDS.has(word) ? ' cs-word--accent' : ''}`}
            >
              {ACCENT_WORDS.has(word) ? <em>{word}</em> : word}
            </span>
          ))}
        </h2>
        <p className="cs-body" ref={bodyRef}>
          Ogni cucciolo Maatilayla trova la famiglia giusta. Scrivici: ti raccontiamo
          chi siamo, come lavoriamo e come puoi prenotare il tuo barboncino toy.
        </p>
        <div className="cs-cta" ref={ctaRef}>
          <Link to="/contatti" className="cs-cta-btn">
            Scrivici
          </Link>
        </div>
      </div>
    </section>
  )
}
```

**Parte B — CSS cs- in src/index.css:**

Aggiungere in fondo a index.css (dopo il blocco wm-):

```css
/* ═══════════════════════════════════════════════════
   CHI SIAMO CTA  (cs-)
   ═══════════════════════════════════════════════════ */

.cs-section {
  position: relative;
  min-height: 480px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  background: var(--color-warm-white);
}

.cs-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  z-index: 0;
  pointer-events: none;
}

.cs-section::after {
  content: '';
  position: absolute;
  inset: 0;
  background: rgba(26, 16, 8, 0.52);
  z-index: 1;
}

.cs-content {
  position: relative;
  z-index: 2;
  text-align: center;
  padding: clamp(3rem, 6vw, 5rem) clamp(1.5rem, 5vw, 4rem);
  max-width: 700px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.25rem;
}

.cs-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(250, 224, 208, 0.2);
  color: var(--color-primary-pale);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  border: 1px solid rgba(250, 224, 208, 0.3);
}

.cs-title {
  font-family: var(--font-heading);
  font-size: clamp(1.9rem, 3.5vw, 2.8rem);
  font-weight: 700;
  color: #fff;
  line-height: 1.2;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.3em;
}

.cs-word { display: inline-block; }

.cs-word--accent em {
  font-style: italic;
  color: var(--color-primary-pale);
}

.cs-body {
  font-family: var(--font-body);
  font-size: clamp(0.9rem, 1.1vw, 1.05rem);
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.75;
  max-width: 540px;
}

.cs-cta-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.875rem 2.25rem;
  background: var(--color-primary);
  color: #fff;
  font-family: var(--font-body);
  font-size: 0.9rem;
  font-weight: 600;
  border-radius: var(--radius-btn);
  text-decoration: none;
  transition: background 0.2s ease, transform 0.2s ease;
}

.cs-cta-btn:hover {
  background: var(--color-primary-light);
  transform: translateY(-1px);
}

.cs-cta-btn:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 3px;
}

/* Mobile CSS entrance (quando IntersectionObserver, non GSAP) */
.cs-section:not(.cs-entered) .cs-content {
  opacity: 0;
  translate: 0 20px;
  transition: opacity 0.6s ease, translate 0.6s ease;
}

.cs-section.cs-entered .cs-content {
  opacity: 1;
  translate: none;
}

@media (max-width: 768px) {
  .cs-section {
    min-height: 520px;
  }
}

@media (min-width: 1440px) {
  .cs-section {
    min-height: 560px;
  }
}
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. ChiSiamoCta esporta default. CSS cs- in index.css senza conflitti.</done>
</task>

<task type="auto">
  <name>Task 2: Assemblare tutte le sezioni in ChiSiamo.tsx</name>
  <files>src/pages/ChiSiamo.tsx</files>
  <action>
Sostituire il contenuto di `src/pages/ChiSiamo.tsx` preservando il `<Helmet>` esistente e la `HeroSection` esistente. Aggiungere le 7 nuove sezioni nell'ordine corretto.

**Nota importante sul deep-link id="storia":** la `StoriaLaylaSection` ha già `id="storia"` nel proprio `<section>` element (verificare — se non presente, l'assembly non lo può aggiungere dall'esterno senza prop). Controllare che `StoriaLaylaSection` abbia `id="storia"` oppure aggiungere un div wrapper con `id="storia"` prima del componente.

**Contenuto finale di ChiSiamo.tsx:**

```tsx
import { Helmet } from 'react-helmet-async'
import HeroSection from '@/components/sections/HeroSection'
import StoriaLaylaSection from '@/components/sections/StoriaLaylaSection'
import CredenzialiSection from '@/components/sections/CredenzialiSection'
import ValoriSection from '@/components/sections/ValoriSection'
import StrutturaDSection from '@/components/sections/StrutturaDSection'
import TimelineSection from '@/components/sections/TimelineSection'
import WorldMapSection from '@/components/sections/WorldMapSection'
import ChiSiamoCta from '@/components/sections/ChiSiamoCta'

export default function ChiSiamo() {
  return (
    <>
      <Helmet>
        <title>Chi Siamo — Maatilayla Allevamento Barboncini Toy</title>
        <meta
          name="description"
          content="Scopri la storia dell'allevamento Maatilayla: la nostra passione per i barboncini toy fulvi, la struttura e i nostri obiettivi per cuccioli sani, belli ed equilibrati."
        />
        <meta property="og:title" content="Chi Siamo — Maatilayla" />
        <meta
          property="og:description"
          content="Scopri la storia dell'allevamento Maatilayla: la nostra passione per i barboncini toy fulvi."
        />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="https://allevamentobarboncinimaatilayla.it/chi-siamo" />
        <meta
          property="og:image"
          content="https://allevamentobarboncinimaatilayla.it/content/images/maatilayla-header-chi-siamo.webp"
        />
        <meta property="og:locale" content="it_IT" />
        <meta property="og:site_name" content="Maatilayla" />
      </Helmet>

      <HeroSection
        key="chi-siamo"
        lightText
        image="/content/images/maatilayla-header-chi-siamo.webp"
        alt="Cuccioli neonati di barboncino toy fulvo su copertina maculata"
        badge="La Nostra Filosofia"
        title={
          <>
            Un concetto del tutto{' '}
            <em style={{ fontStyle: 'italic', color: 'var(--color-primary)' }}>naturale.</em>
          </>
        }
        description={{
          short:
            'Una struttura immersa nel verde, dove ogni barboncino cresce in libertà. Tre obiettivi guidano ogni scelta: cuccioli sani, belli ed equilibrati.',
          full: 'Una struttura immersa nel verde della natura, in piena sintonia con lo spirito libero dei nostri barboni. Ogni scelta è guidata da tre obiettivi: cuccioli sani grazie a controlli sanitari rigorosi, belli attraverso una selezione attenta, ed equilibrati con il programma Biosensor e tre mesi di socializzazione in famiglia.',
        }}
        cta={[
          { label: 'Struttura', to: '/chi-siamo#struttura', variant: 'primary' },
          { label: 'Storia', to: '/chi-siamo#storia', variant: 'outline' },
        ]}
      />

      <StoriaLaylaSection />
      <CredenzialiSection />
      <ValoriSection />
      <StrutturaDSection />
      <TimelineSection />
      <WorldMapSection />
      <ChiSiamoCta />
    </>
  )
}
```

**Verifica deep-link:** Se `StoriaLaylaSection` non ha `id="storia"` nel suo `<section>`, aggiungere un div wrapper nell'assembly:
```tsx
<div id="storia">
  <StoriaLaylaSection />
</div>
```
Preferire però che il componente stesso abbia l'id (più semantico). Controllare il file generato in Plan 01 e aggiungere `id="storia"` alla `<section>` di StoriaLaylaSection se mancante.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. ChiSiamo.tsx importa tutti e 8 i componenti. Il file è ~60 righe (compatto, solo imports e layout).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Pagina Chi Siamo completa con 8 sezioni:
1. Hero (pre-esistente)
2. Storia Layla — racconto narrativo con foto placeholder
3. Credenziali — 3 card ENCI di Layla Zarfati
4. Filosofia e Valori — griglia 3×2 con 6 pillars
5. La Struttura — testo + foto + video YouTube lazy
6. Timeline — 5 tappe orizzontali 2018-2020
7. Mappa del Mondo — SVG interattiva con marker placeholder
8. CTA finale — foto background + link a /contatti
  </what-built>
  <how-to-verify>
1. Avvia `npm run dev`
2. Apri http://localhost:5173/chi-siamo
3. Scorrere tutta la pagina dall'alto in basso — verificare:
   - Ogni sezione è visibile e stilizzata correttamente
   - Le animazioni entrance funzionano scorrendo (fade-up al primo scroll)
   - Scrollare di nuovo verso l'alto e poi giù: le animazioni si ripetono (bidirezionali)
4. Cliccare il CTA Hero "Struttura" — deve scorrere a #struttura
5. Cliccare il CTA Hero "Storia" — deve scorrere a #storia
6. Nella sezione Struttura: cliccare il play button YouTube — deve avviarsi il video
7. Verificare la mappa: continenti visibili in beige, dot rossi sui paesi placeholder
8. Cliccare "Scrivici" nella CTA finale — deve navigare a /contatti
9. Ridimensionare a 375px (mobile): verificare responsive su tutte le sezioni
  </how-to-verify>
  <resume-signal>Digita "approvato" se tutto è corretto, oppure descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. Pagina /chi-siamo con 8 sezioni nell'ordine corretto
3. Deep-links #storia e #struttura funzionanti
4. YouTube facade: nessuna risorsa YouTube caricata fino al click
5. Mappa SVG: paesi in --color-accent, marker in --color-primary
6. CTA finale: link funzionante verso /contatti
7. Checkpoint: utente conferma visivamente la pagina completa
</verification>

<success_criteria>
- ChiSiamoCta.tsx esiste ed esporta il componente default
- ChiSiamo.tsx importa e renderizza tutte le 8 sezioni nell'ordine corretto
- Build production pulita (zero errori)
- Pagina Chi Siamo visivamente completa e responsive
- Deep-links Hero funzionanti
- Animazioni bidirezionali su tutte le sezioni
- Utente ha approvato il checkpoint visivo
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-07-SUMMARY.md`
</output>
