---
phase: 04-pagine-interne
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/sections/StrutturaDSection.tsx
  - src/index.css
autonomous: true
requirements: [CS-04]

must_haves:
  truths:
    - "L'utente vede badge + h2 + testo descrittivo + immagine struttura + video YouTube lazy"
    - "Il video YouTube non carica nessuna risorsa YouTube fino al click dell'utente"
    - "Al click sul play button il video si avvia con autoplay all'interno della sezione"
    - "Il layout è due colonne su desktop: testo a sinistra, media (foto+video) a destra"
    - "L'animazione entrance IntersectionObserver bidirezionale funziona"
  artifacts:
    - path: "src/components/sections/StrutturaDSection.tsx"
      provides: "Sezione struttura fisica dell'allevamento con video YouTube lazy"
      exports: ["default StrutturaDSection"]
    - path: "src/index.css"
      provides: "CSS prefisso st- e yt- per StrutturaDSection"
      contains: ".st-section"
  key_links:
    - from: "src/components/sections/StrutturaDSection.tsx"
      to: "https://www.youtube.com/embed/_jQ2oFcZIhY"
      via: "stato React useState — iframe caricato solo al click"
      pattern: "youtube.com/embed/_jQ2oFcZIhY"
---

<objective>
Costruire StrutturaDSection: layout due colonne con testo descrittivo + immagine casetta + video YouTube lazy embed.

Purpose: Quarta sezione della pagina Chi Siamo. Mostra il posto fisico oggi — prima di raccontare con la timeline come ci si è arrivati. Il video del campo agility è la prova visiva del progetto compiuto.
Output: Componente con YouTube facade click-to-load (nessun JS YouTube fino al click), CSS in index.css, pronto per import in ChiSiamo.tsx.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md
@.planning/phases/04-pagine-interne/04-RESEARCH.md

@src/components/sections/AllevamentoSection.tsx
@content/testi-sito-attuale/la-struttura.md

<interfaces>
<!-- YouTube VIDEO_ID: _jQ2oFcZIhY -->
<!-- Pattern YouTube facade da 04-RESEARCH.md:
  - useState active: false
  - inactive: <button className="yt-facade"> con thumbnail img da img.youtube.com/vi/{id}/sddefault.jpg
  - active: <div className="yt-wrapper"><iframe src="embed/{id}?autoplay=1&rel=0" /></div>
  - Il click sul button è user gesture → autoplay funziona su Chrome/Safari
  - Usare youtube-nocookie.com per privacy by design
-->

<!-- Immagine struttura disponibile: nessuna foto specifica struttura in public/content/images/
  Usare maatilayla-barboncino-toy-relax-sole.webp o simile come placeholder
  fino a che l'utente non fornisce la foto della casetta dei barboni -->

<!-- Pattern sezione due colonne da AllevamentoSection:
  grid-template-columns: 1fr 1fr
  gap: clamp(2.5rem, 5vw, 5rem)
  align-items: center
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare StrutturaDSection con testo + foto + YouTube facade</name>
  <files>src/components/sections/StrutturaDSection.tsx</files>
  <action>
Creare il componente `StrutturaDSection` con layout due colonne: testo a sinistra, stack foto+video a destra.

**YouTube VIDEO_ID:** `_jQ2oFcZIhY`

**Struttura JSX:**
```tsx
'use client'
import { useState, useEffect, useRef } from 'react'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

const VIDEO_ID = '_jQ2oFcZIhY'

export default function StrutturaDSection() {
  const [videoActive, setVideoActive] = useState(false)
  const sectionRef = useRef<HTMLElement>(null)
  const blockRef = useRef<HTMLDivElement>(null)
  const imgRef = useRef<HTMLDivElement>(null)

  // IntersectionObserver entrance
  useEffect(() => {
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (prefersReduced) {
      blockRef.current?.classList.add('st-entered')
      return
    }
    const observer = new IntersectionObserver(
      ([entry]) => {
        requestAnimationFrame(() => {
          if (entry.isIntersecting) {
            blockRef.current?.classList.add('st-entered')
          } else {
            blockRef.current?.classList.remove('st-entered')
          }
        })
      },
      { threshold: 0, rootMargin: '-12% 0px' },
    )
    if (blockRef.current) observer.observe(blockRef.current)
    return () => observer.disconnect()
  }, [])

  // GSAP parallax sull'immagine
  useEffect(() => {
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (prefersReduced) return
    const ctx = gsap.context(() => {
      const img = imgRef.current?.querySelector('img')
      if (img && blockRef.current) {
        gsap.fromTo(img, { yPercent: -8 }, {
          yPercent: 8,
          ease: 'none',
          force3d: true,
          scrollTrigger: {
            trigger: blockRef.current,
            start: 'top bottom',
            end: 'bottom top',
            scrub: 0.6,
          },
        })
      }
    }, sectionRef)
    return () => ctx.revert()
  }, [])

  return (
    <section id="struttura" className="st-section" ref={sectionRef} aria-label="La struttura di Maatilayla">
      <div className="st-block" ref={blockRef}>
        {/* Colonna sinistra: testo */}
        <div className="st-content">
          <span className="st-badge">La Struttura</span>
          <h2 className="st-title">
            Uno spazio pensato <em className="st-accent">per loro.</em>
          </h2>
          <p className="st-body">
            Una proprietà immersa nel verde, dove i nostri barboncini toy vivono in piena libertà.
            Ampi spazi aperti per correre, un'area interna confortevole e una casetta dedicata
            dove mangiano, vengono curati e riposano con vista sui boschi circostanti.
          </p>
          <p className="st-body">
            Nel 2020 abbiamo completato il campo di agility — un'area attrezzata con ostacoli,
            bascule, cerchi, slalom e tunnel. Non solo un campo sportivo: un modo per stimolare
            la mente dei nostri barboni e mantenerli felici ogni giorno.
          </p>
          <p className="st-body st-body--em">
            Tutto è stato costruito con un solo obiettivo: offrire ai nostri cani la vita che
            meritano — naturale, serena e ricca di stimoli.
          </p>
        </div>

        {/* Colonna destra: foto + video */}
        <div className="st-media">
          <div className="st-img" ref={imgRef}>
            <img
              src="/content/images/maatilayla-barboncino-toy-relax-sole.webp"
              alt="Barboncino toy dell'allevamento Maatilayla che si gode il sole nel campo"
              loading="lazy"
              decoding="async"
            />
          </div>

          {/* YouTube facade click-to-load */}
          {!videoActive ? (
            <button
              className="yt-facade"
              onClick={() => setVideoActive(true)}
              aria-label="Guarda il video del campo agility Maatilayla"
            >
              <img
                src={`https://img.youtube.com/vi/${VIDEO_ID}/sddefault.jpg`}
                alt="Anteprima video campo agility barboncini Maatilayla"
                loading="lazy"
                decoding="async"
              />
              <span className="yt-play-btn" aria-hidden="true">
                <svg viewBox="0 0 68 48" width="68" height="48">
                  <path d="M66.52 7.74c-.78-2.93-2.49-5.41-5.42-6.19C55.79.13 34 0 34 0S12.21.13 6.9 1.55c-2.93.78-4.63 3.26-5.42 6.19C.06 13.05 0 24 0 24s.06 10.95 1.48 16.26c.78 2.93 2.49 5.41 5.42 6.19C12.21 47.87 34 48 34 48s21.79-.13 27.1-1.55c2.93-.78 4.64-3.26 5.42-6.19C67.94 34.95 68 24 68 24s-.06-10.95-1.48-16.26z" fill="#f00"/>
                  <path d="M45 24 27 14v20" fill="#fff"/>
                </svg>
              </span>
            </button>
          ) : (
            <div className="yt-wrapper">
              <iframe
                src={`https://www.youtube-nocookie.com/embed/${VIDEO_ID}?autoplay=1&rel=0`}
                title="Video campo agility allevamento Maatilayla barboncini toy"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                allowFullScreen
                loading="lazy"
              />
            </div>
          )}
        </div>
      </div>
    </section>
  )
}
```

Note implementazione:
- Usare `youtube-nocookie.com` (privacy by design, confermato in RESEARCH.md)
- `autoplay=1` nel src dell'iframe (il click dell'utente è già la user gesture necessaria)
- Il blocco `'use client'` non è necessario in React normale — rimuoverlo se genera warning TypeScript
- `st-body--em` per il paragrafo conclusivo in corsivo muted
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. Componente esporta `default StrutturaDSection`. Lo stato videoActive gestisce correttamente il toggle facade → iframe.</done>
</task>

<task type="auto">
  <name>Task 2: CSS prefissi st- e yt- in src/index.css</name>
  <files>src/index.css</files>
  <action>
Aggiungere in fondo a `src/index.css` (dopo i blocchi vl-) il CSS per `StrutturaDSection` con prefissi `st-` e `yt-`.

**Regole da aggiungere:**

```css
/* ═══════════════════════════════════════════════════
   STRUTTURA SECTION  (st-)
   ═══════════════════════════════════════════════════ */

.st-section {
  background: var(--color-cream);
  padding: clamp(4rem, 8vw, 7rem) 0;
}

.st-block {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(1.5rem, 5vw, 4rem);
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(2.5rem, 5vw, 5rem);
  align-items: center;
}

/* Contenuto testuale */
.st-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  margin-bottom: 1.25rem;
}

.st-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-primary);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}

.st-title {
  font-family: var(--font-heading);
  font-size: clamp(1.7rem, 3vw, 2.5rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.2;
  margin-bottom: 1.5rem;
}

.st-accent {
  font-style: italic;
  color: var(--color-primary);
}

.st-body {
  font-family: var(--font-body);
  font-size: clamp(0.9rem, 1.1vw, 1rem);
  color: var(--color-text-muted);
  line-height: 1.8;
  margin-bottom: 1rem;
}

.st-body--em {
  font-style: italic;
  color: var(--color-text);
  font-weight: 500;
}

/* Colonna media */
.st-media {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.st-img {
  position: relative;
  border-radius: var(--radius-card);
  overflow: hidden;
  aspect-ratio: 16 / 9;
  background: var(--color-primary-pale);
  contain: layout style paint;
}

.st-img img {
  width: 100%;
  height: 120%;
  object-fit: cover;
  object-position: center 30%;
  display: block;
  translate: 0 -10%;
}

/* ── YouTube facade  (yt-) ── */
.yt-wrapper {
  position: relative;
  aspect-ratio: 16 / 9;
  border-radius: var(--radius-card);
  overflow: hidden;
  background: #000;
}

.yt-wrapper iframe {
  width: 100%;
  height: 100%;
  border: 0;
  display: block;
}

.yt-facade {
  position: relative;
  display: block;
  aspect-ratio: 16 / 9;
  width: 100%;
  border-radius: var(--radius-card);
  overflow: hidden;
  cursor: pointer;
  border: 0;
  padding: 0;
  background: #000;
  contain: layout style paint;
}

.yt-facade img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: opacity 0.2s ease;
}

.yt-facade:hover img {
  opacity: 0.85;
}

.yt-play-btn {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}

/* ── Animazione entrata ── */
.st-block {
  opacity: 0;
  translate: 0 28px;
  transition:
    opacity 0.7s ease,
    translate 0.7s ease;
}

.st-block.st-entered {
  opacity: 1;
  translate: none;
}

.st-block.st-entered .st-content > :nth-child(1) { transition-delay: 0s; }
.st-block.st-entered .st-content > :nth-child(2) { transition-delay: 0.08s; }
.st-block.st-entered .st-content > :nth-child(3) { transition-delay: 0.15s; }
.st-block.st-entered .st-content > :nth-child(4) { transition-delay: 0.22s; }
.st-block.st-entered .st-content > :nth-child(5) { transition-delay: 0.29s; }
.st-block.st-entered .st-media           { transition-delay: 0.1s; }

/* ── Responsive ── */
@media (max-width: 768px) {
  .st-block {
    grid-template-columns: 1fr;
  }

  .st-img {
    aspect-ratio: 3 / 2;
  }
}

@media (min-width: 1440px) {
  .st-block {
    padding: 0 3rem;
  }
}
```

Aggiungere dopo il blocco vl- già presente in index.css. Non modificare CSS esistente.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa. Classi st- e yt- presenti in index.css. Il YouTube facade mostra thumbnail e play button prima del click; dopo il click mostra iframe.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. Prima del click: thumbnail YouTube visibile con play button SVG rosso, nessuna risorsa YouTube caricata nel Network tab
3. Dopo il click: iframe YouTube si avvia con autoplay
4. Layout due colonne su desktop: testo sinistra, media destra
5. Stack colonna su mobile (max-width 768px)
</verification>

<success_criteria>
- StrutturaDSection.tsx esiste ed esporta il componente default
- CSS st- e yt- in index.css senza conflitti
- Build production pulita (zero errori)
- YouTube facade funzionante: nessun caricamento risorse YouTube finché l'utente non clicca
- id="struttura" sul section per deep-link dal Hero CTA "Struttura"
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-04-SUMMARY.md`
</output>
