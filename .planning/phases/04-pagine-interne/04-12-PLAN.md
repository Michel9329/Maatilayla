---
phase: 04-pagine-interne
plan: 12
type: execute
wave: 2
depends_on: [04-11]
files_modified:
  - src/pages/Galleria.tsx
  - src/index.css
autonomous: false
requirements:
  - GAL-04
  - GAL-05

must_haves:
  truths:
    - "Pagina Galleria mostra Hero + GallerySection + CTA + ContactSection + InstagramFeed"
    - "CTA sezione invita a contattare l'allevamento con link a /contatti"
    - "SEO completo: Helmet con title, description, OG tags, canonical"
    - "Utente puo' navigare l'intera pagina galleria senza errori"
  artifacts:
    - path: "src/pages/Galleria.tsx"
      provides: "Assembly pagina completa con tutte le sezioni"
      min_lines: 50
    - path: "src/index.css"
      provides: "CSS gc-* per CTA galleria (se non riusa pattern esistente)"
      contains: ".gc-"
  key_links:
    - from: "src/pages/Galleria.tsx"
      to: "src/components/sections/GallerySection.tsx"
      via: "import GallerySection"
      pattern: "import GallerySection"
    - from: "src/pages/Galleria.tsx"
      to: "src/components/sections/ContactSection.tsx"
      via: "import ContactSection"
      pattern: "import ContactSection"
---

<objective>
Assemblare la pagina Galleria completa con tutte le sezioni e verificare visivamente.

Purpose: Collegare GallerySection (creato in 04-11) nella pagina Galleria.tsx, aggiungere CTA verso contatti, riutilizzare ContactSection e InstagramFeedSection dal pattern Homepage/Chi Siamo, completare SEO.

Output: Galleria.tsx completa, CTA CSS, checkpoint visivo
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pagine-interne/04-11-SUMMARY.md
@.planning/phases/04-pagine-interne/04-GALLERIA-RESEARCH.md
@src/pages/Galleria.tsx
@src/pages/ChiSiamo.tsx
@src/components/sections/ChiSiamoCta.tsx
</context>

<interfaces>
<!-- Componenti esistenti da riusare -->

From src/components/sections/HeroSection.tsx:
- Props: image, alt, title, subtitle, ctaText?, ctaTo?

From src/components/sections/ContactSection.tsx:
- Componente riusabile, accetta prop `variant?: 'default' | 'green'`
- Default: colori primary standard

From src/components/sections/InstagramFeedSection.tsx:
- Componente senza props, marquee full-width

From src/components/sections/GallerySection.tsx (creato in 04-11):
- Componente senza props, contiene griglia masonry + filtri + lightbox

From src/data/galleryData.ts (creato in 04-11):
- export type GalleryCategory
- export interface GalleryPhoto
- export const galleryPhotos: GalleryPhoto[]
- export const galleryCategories
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Assembly pagina Galleria + CTA sezione</name>
  <files>src/pages/Galleria.tsx, src/index.css</files>
  <action>
**Aggiornare `src/pages/Galleria.tsx`:**

1. **Imports**: aggiungere lazy imports per GallerySection, ContactSection, InstagramFeedSection. HeroSection gia' importato.

2. **Helmet SEO** (aggiornare l'esistente):
   - Title: "Galleria — Maatilayla Allevamento Barboncini Toy"
   - Description: aggiornare con menzione filtri e categorie
   - Aggiungere `<link rel="canonical" href="https://allevamentobarboncinimaatilayla.it/galleria" />`
   - OG tags gia' presenti — verificare completezza

3. **Ordine sezioni**:
   - HeroSection (gia' presente) — title "Galleria.", subtitle "Scatti e momenti dall'allevamento Maatilayla."
   - GallerySection (nuovo)
   - CTA Galleria (nuovo — sezione inline, non componente separato)
   - ContactSection (riuso)
   - InstagramFeedSection (riuso)

4. **CTA Galleria** — creare sezione CTA inline nella pagina (pattern simile a ChiSiamoCta ma piu' semplice, senza foto background):
   - Wrapper `.gc-wrap` con padding e sfondo cream
   - Container `.gc-section` con max-width 1400px, border-radius card, sfondo warm-white, padding generoso, text-align center
   - Badge "Ti piace quello che vedi?" con dot pulsante
   - Titolo: "Vieni a conoscerci di persona." con font-heading
   - Testo: breve invito a visitare l'allevamento (1-2 righe)
   - Bottone CTA: "Contattaci" link a /contatti, stile primary (bg primary, color white, radius btn, hover translateY(-1px))
   - IntersectionObserver entrance (fade-in 0.7s) con check prefers-reduced-motion
   - CSS responsive (padding ridotto su mobile)

5. **CSS in index.css** — aggiungere classi `gc-*`:
   - `.gc-wrap`: padding 2rem 1.5rem, bg cream
   - `.gc-section`: max-width 900px, margin auto, border-radius card, bg warm-white, padding 3.5rem 2rem, text-align center, opacity 0 translateY(20px), transition 0.7s
   - `.gc-section.gc-entered`: opacity 1, translateY(0)
   - `.gc-badge`: stile badge standard (0.65rem, 600, uppercase, dot primary)
   - `.gc-title`: font-heading, clamp(1.6rem, 2.5vw, 2.2rem), 700
   - `.gc-body`: clamp(0.9rem, 1.1vw, 1.05rem), text-muted, line-height 1.65, max-width 520px, margin auto
   - `.gc-cta-btn`: inline-block, padding 0.75rem 2rem, bg primary, color white, radius btn, font 0.9rem/600, text-decoration none, transition all 0.25s, hover translateY(-1px) + bg primary-light
   - `.gc-cta-btn:focus-visible`: outline 2px primary, offset 2px
   - Media 768px: gc-wrap padding 3rem 2rem, gc-section padding 4rem 3rem
   - `@media (prefers-reduced-motion: reduce)`: gc-section opacity 1/transform none/transition none
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Galleria.tsx renderizza Hero + GallerySection + CTA + ContactSection + InstagramFeed. Build production pulita. CTA ha link funzionante a /contatti. SEO Helmet completo con canonical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visivo pagina Galleria</name>
  <what-built>Pagina Galleria completa: griglia masonry con foto placeholder, filtri categoria, lightbox YARL fullscreen, CTA verso contatti, ContactSection e Instagram feed.</what-built>
  <how-to-verify>
    1. Avviare dev server: `npm run dev`
    2. Navigare a http://localhost:5173/galleria
    3. Verificare Hero section con titolo "Galleria."
    4. Verificare griglia masonry con foto placeholder (2 col mobile, 3 tablet, 4 desktop)
    5. Cliccare sui filtri categoria — la griglia si aggiorna, il contatore foto cambia
    6. Cliccare su una foto — si apre lightbox fullscreen con thumbnails in basso, zoom funzionante, counter "1/N"
    7. Nel lightbox: navigare con frecce, zoom con scroll/pinch, chiudere con X o Escape
    8. Verificare CTA "Vieni a conoscerci di persona" con bottone "Contattaci" che linka a /contatti
    9. Verificare ContactSection e Instagram feed in fondo
    10. Ridimensionare browser per verificare responsive (mobile stack, tablet 3col, desktop 4col)
    11. Le foto sono placeholder — e' normale che non si carichino. La struttura e il layout devono essere corretti.
  </how-to-verify>
  <resume-signal>Digita "approvato" o descrivi i problemi da correggere</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- Pagina /galleria carica tutte le sezioni nell'ordine corretto
- Filtri cambiano il contenuto della griglia
- Lightbox si apre sulla foto corretta (anche dopo filtro)
- CTA linka a /contatti
- Responsive: 2/3/4 colonne
</verification>

<success_criteria>
- Galleria.tsx assemblata con 5 sezioni (Hero, Gallery, CTA, Contact, Instagram)
- CTA con badge, titolo, testo e bottone link a /contatti
- SEO completo (Helmet + canonical + OG)
- Build production pulita
- Checkpoint visivo approvato dall'utente
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-12-SUMMARY.md`
</output>
