---
phase: 04-pagine-interne
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/sections/TimelineSection.tsx
  - src/index.css
autonomous: true
requirements: [CS-05]

must_haves:
  truths:
    - "L'utente vede 5 tappe cronologiche in layout orizzontale con linea di connessione e dot"
    - "Su desktop tutte le tappe sono visibili senza scroll"
    - "Su mobile una tappa per volta con scroll-snap e dot indicatori"
    - "Ogni tappa ha anno, titolo milestone, testo descrittivo"
    - "L'animazione entrance IntersectionObserver bidirezionale funziona sull'intera sezione"
  artifacts:
    - path: "src/components/sections/TimelineSection.tsx"
      provides: "Timeline orizzontale 5 tappe storia struttura Maatilayla"
      exports: ["default TimelineSection"]
    - path: "src/index.css"
      provides: "CSS prefisso tl- per TimelineSection"
      contains: ".tl-section"
  key_links:
    - from: "src/components/sections/TimelineSection.tsx"
      to: "src/index.css"
      via: "classi CSS tl-"
      pattern: "tl-section|tl-track|tl-item|tl-entered"
---

<objective>
Costruire TimelineSection: timeline orizzontale con 5 tappe della storia della struttura Maatilayla (2018–2020).

Purpose: Quinta sezione della pagina Chi Siamo. Dopo aver visto com'è oggi la struttura (StrutturaDSection), la timeline racconta come ci si è arrivati — un viaggio da zero a campo agility completo.
Output: Componente CSS-only (no librerie), scroll-snap mobile, dot indicatori, pronto per import in ChiSiamo.tsx.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md
@.planning/phases/04-pagine-interne/04-RESEARCH.md

@content/testi-sito-attuale/la-struttura.md

<interfaces>
<!-- 5 tappe da la-struttura.md + CONTEXT.md:
  1. "Prima del 2018" — "La fondazione" — storia di Jolie, la passione che ha dato origine a Maatilayla
  2. "Ottobre 2018"   — "Il trasferimento" — lasciare la casa di città per spazi aperti
  3. "Novembre 2018"  — "I lavori iniziano" — i cani esplorano il nuovo terreno liberi
  4. "Ottobre 2019"   — "La casetta dei barboni" — inaugurazione spazio dedicato
  5. "Maggio 2020"    — "Il campo di agility" — area attrezzata completa

  Struttura array: { year, milestone, description }
-->

<!-- Pattern CSS timeline da 04-RESEARCH.md (già dettagliato):
  .tl-track: display flex, overflow-x auto, scroll-snap-type x mandatory
  .tl-item: flex 0 0 clamp(220px, 22vw, 260px) su desktop, 85vw su mobile
  .tl-dot: 14px cerchio primary, bordo cream, z-index 1
  .tl-track::before: linea orizzontale gradient trasparente → primary-pale → trasparente
-->

<!-- Dot indicatori mobile: stesso pattern PedigreeSection (dots CSS, activeIndex via useState + scroll listener) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare TimelineSection con scroll-snap mobile e dot indicatori</name>
  <files>src/components/sections/TimelineSection.tsx</files>
  <action>
Creare il componente `TimelineSection` con array di tappe, layout orizzontale CSS, scroll-snap mobile, dot indicatori sincronizzati con lo scroll.

**Struttura dati (array nel componente):**
```tsx
const milestones = [
  {
    year: 'Prima del 2018',
    milestone: 'La Fondazione.',
    description: 'Tutto nasce da Jolie — una barboncina fulva che ha stregato Layla per sempre. Da quel momento, l\'idea di creare un allevamento serio e appassionato prende forma.',
  },
  {
    year: 'Ottobre 2018',
    milestone: 'Il Trasferimento.',
    description: 'La passione per i barboncini toy spinge a lasciare la casa di città. Un nuovo spazio, più grande e naturale, aspetta i nostri cani.',
  },
  {
    year: 'Novembre 2018',
    milestone: 'I Lavori Iniziano.',
    description: 'Inizia la ristrutturazione, ma i cani sono già felici: spazi sconfinati da esplorare, insetti da inseguire, natura vera. Fanno i cani, finalmente.',
  },
  {
    year: 'Ottobre 2019',
    milestone: 'La Casetta dei Barboni.',
    description: 'I lavori sono finiti. La nuova casetta dedicata ai barboncini viene inaugurata: è il loro angolo, dove mangiano, vengono curati e riposano.',
  },
  {
    year: 'Maggio 2020',
    milestone: 'Il Campo di Agility.',
    description: 'Un\'ampia area attrezzata con ostacoli, bascule, cerchi, slalom e tunnel. I nostri cani sono davvero felici. Il sogno è completo.',
  },
]
```

**Struttura JSX:**
```tsx
import { useEffect, useRef, useState, useCallback } from 'react'

export default function TimelineSection() {
  const sectionRef = useRef<HTMLElement>(null)
  const containerRef = useRef<HTMLDivElement>(null)
  const trackRef = useRef<HTMLDivElement>(null)
  const [activeIndex, setActiveIndex] = useState(0)

  // IntersectionObserver entrance
  useEffect(() => {
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (prefersReduced) {
      containerRef.current?.classList.add('tl-entered')
      return
    }
    const observer = new IntersectionObserver(
      ([entry]) => {
        requestAnimationFrame(() => {
          if (entry.isIntersecting) {
            containerRef.current?.classList.add('tl-entered')
          } else {
            containerRef.current?.classList.remove('tl-entered')
          }
        })
      },
      { threshold: 0, rootMargin: '-10% 0px' },
    )
    if (containerRef.current) observer.observe(containerRef.current)
    return () => observer.disconnect()
  }, [])

  // Dot indicatori: sincronizzati con scroll
  const handleScroll = useCallback(() => {
    const track = trackRef.current
    if (!track) return
    const itemWidth = track.scrollWidth / milestones.length
    const index = Math.round(track.scrollLeft / itemWidth)
    setActiveIndex(Math.min(index, milestones.length - 1))
  }, [])

  useEffect(() => {
    const track = trackRef.current
    if (!track) return
    track.addEventListener('scroll', handleScroll, { passive: true })
    return () => track.removeEventListener('scroll', handleScroll)
  }, [handleScroll])

  return (
    <section className="tl-section" ref={sectionRef} aria-label="La storia di Maatilayla">
      <div className="tl-container" ref={containerRef}>
        <div className="tl-header">
          <span className="tl-badge">La Timeline</span>
          <h2 className="tl-title">
            Da un sogno a <em className="tl-accent">una realtà compiuta.</em>
          </h2>
        </div>

        <div className="tl-track" ref={trackRef} role="list">
          {milestones.map((m, i) => (
            <div key={m.year} className="tl-item" role="listitem">
              <div className="tl-dot" aria-hidden="true" />
              <span className="tl-year">{m.year}</span>
              <h3 className="tl-milestone">{m.milestone}</h3>
              <p className="tl-desc">{m.description}</p>
            </div>
          ))}
        </div>

        {/* Dot indicatori — visibili solo su mobile */}
        <div className="tl-dots" aria-hidden="true">
          {milestones.map((_, i) => (
            <span
              key={i}
              className={`tl-dot-indicator${i === activeIndex ? ' tl-dot-indicator--active' : ''}`}
            />
          ))}
        </div>
      </div>
    </section>
  )
}
```

**Note:**
- Il `handleScroll` calcola l'indice attivo in modo semplice (scrollLeft / larghezza_item arrotondata)
- I dot indicatori sono nascosti su desktop via CSS (`display: none` sopra 768px)
- Il `role="list"` + `role="listitem"` per accessibilità screen reader
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. Componente esporta `default TimelineSection`. Array milestones ha 5 elementi con tutti i campi.</done>
</task>

<task type="auto">
  <name>Task 2: CSS prefisso tl- in src/index.css</name>
  <files>src/index.css</files>
  <action>
Aggiungere in fondo a `src/index.css` (dopo i blocchi yt-/st-) il CSS per `TimelineSection` con prefisso `tl-`. Usare il pattern da 04-RESEARCH.md.

**Regole da aggiungere:**

```css
/* ═══════════════════════════════════════════════════
   TIMELINE SECTION  (tl-)
   ═══════════════════════════════════════════════════ */

.tl-section {
  background: var(--color-warm-white);
  padding: clamp(4rem, 8vw, 7rem) 0;
  overflow: hidden;
}

.tl-container {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: clamp(2rem, 3.5vw, 3rem);
}

/* Header */
.tl-header {
  text-align: center;
  padding: 0 clamp(1.5rem, 5vw, 4rem);
}

.tl-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  margin-bottom: 1.25rem;
}

.tl-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-primary);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}

.tl-title {
  font-family: var(--font-heading);
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.25;
}

.tl-accent {
  font-style: italic;
  color: var(--color-primary);
}

/* Track orizzontale */
.tl-track {
  display: flex;
  gap: 0;
  position: relative;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
  overscroll-behavior-x: contain;
  scrollbar-width: none;
  padding: 2.5rem clamp(1.5rem, 5vw, 4rem) 1rem;
}

.tl-track::-webkit-scrollbar {
  display: none;
}

/* Linea di connessione */
.tl-track::before {
  content: '';
  position: absolute;
  top: 2.5rem;
  left: clamp(1.5rem, 5vw, 4rem);
  right: clamp(1.5rem, 5vw, 4rem);
  height: 2px;
  background: linear-gradient(
    to right,
    transparent,
    var(--color-primary-pale) 5%,
    var(--color-primary-pale) 95%,
    transparent
  );
  pointer-events: none;
  z-index: 0;
}

/* Singola tappa */
.tl-item {
  flex: 0 0 clamp(220px, 22vw, 260px);
  scroll-snap-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.75rem;
  padding: 0 1.25rem;
  text-align: center;
}

/* Dot sulla linea */
.tl-dot {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--color-primary);
  border: 3px solid var(--color-cream);
  box-shadow: 0 0 0 2px var(--color-primary);
  flex-shrink: 0;
  z-index: 1;
  position: relative;
}

/* Anno */
.tl-year {
  font-family: var(--font-heading);
  font-size: clamp(1rem, 1.5vw, 1.2rem);
  font-weight: 700;
  color: var(--color-primary);
  line-height: 1.2;
}

/* Titolo tappa */
.tl-milestone {
  font-family: var(--font-heading);
  font-size: clamp(0.875rem, 1.1vw, 1rem);
  font-weight: 600;
  color: var(--color-text);
  line-height: 1.3;
}

/* Descrizione */
.tl-desc {
  font-family: var(--font-body);
  font-size: clamp(0.775rem, 0.85vw, 0.85rem);
  color: var(--color-text-muted);
  line-height: 1.65;
}

/* Dot indicatori mobile */
.tl-dots {
  display: none;   /* nascosti su desktop */
  justify-content: center;
  gap: 0.5rem;
  padding-bottom: 0.5rem;
}

.tl-dot-indicator {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--color-primary-pale);
  transition: background 0.25s ease, transform 0.25s ease;
  flex-shrink: 0;
}

.tl-dot-indicator--active {
  background: var(--color-primary);
  transform: scale(1.3);
}

/* ── Animazione entrata ── */
.tl-container {
  opacity: 0;
  translate: 0 24px;
  transition:
    opacity 0.65s ease,
    translate 0.65s ease;
}

.tl-container.tl-entered {
  opacity: 1;
  translate: none;
}

.tl-container.tl-entered .tl-header { transition-delay: 0s; }
.tl-container.tl-entered .tl-track  { transition-delay: 0.12s; }
.tl-container.tl-entered .tl-dots   { transition-delay: 0.2s; }

/* ── Responsive ── */
@media (max-width: 768px) {
  .tl-item {
    flex: 0 0 85vw;
  }

  .tl-dots {
    display: flex;
  }
}

@media (min-width: 1440px) {
  .tl-track {
    padding-left: 3rem;
    padding-right: 3rem;
  }

  .tl-track::before {
    left: 3rem;
    right: 3rem;
  }
}
```

Aggiungere dopo il blocco st-/yt- già presente in index.css. Non modificare CSS esistente.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa. Classi tl- presenti in index.css. Su desktop 5 tappe tutte visibili in riga orizzontale. Su mobile scroll-snap con dot indicatori.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. Desktop: tutte e 5 le tappe visibili senza scroll, linea di connessione orizzontale tra i dot
3. Mobile (DevTools 375px): una tappa per volta, scroll-snap, dot indicatori sincronizzati
4. IntersectionObserver: replay animazione al ri-scroll
</verification>

<success_criteria>
- TimelineSection.tsx esiste ed esporta il componente default
- CSS tl- in index.css senza conflitti
- Build production pulita (zero errori)
- 5 tappe (Pre-2018, Ott 2018, Nov 2018, Ott 2019, Mag 2020) con testo sintetico italiano
- Struttura array configurabile per future tappe da aggiungere
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-05-SUMMARY.md`
</output>
