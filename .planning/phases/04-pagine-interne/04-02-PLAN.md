---
phase: 04-pagine-interne
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/CredenzialiSection.tsx
  - src/index.css
autonomous: true
requirements: [CS-02]

must_haves:
  truths:
    - "L'utente vede 3 credenziali ENCI di Layla Zarfati in card visivamente distinte"
    - "Il nome 'Layla Zarfati' è in evidenza come titolo principale della sezione"
    - "Ogni credenziale ha icona Lucide, titolo e breve descrizione"
    - "La sezione ha peso visivo autonomo — non integrata nel testo circostante"
    - "L'animazione entrance IntersectionObserver bidirezionale funziona"
  artifacts:
    - path: "src/components/sections/CredenzialiSection.tsx"
      provides: "Sezione credenziali professionali Layla Zarfati"
      exports: ["default CredenzialiSection"]
    - path: "src/index.css"
      provides: "CSS prefisso cr- per CredenzialiSection"
      contains: ".cr-section"
  key_links:
    - from: "src/components/sections/CredenzialiSection.tsx"
      to: "src/index.css"
      via: "classi CSS cr-"
      pattern: "cr-section|cr-card|cr-entered"
---

<objective>
Costruire CredenzialiSection: sezione autonoma con le 3 credenziali ENCI di Layla Zarfati.

Purpose: Seconda sezione della pagina Chi Siamo. Dopo l'emozione della storia, questa sezione ristabilisce l'autorevolezza professionale — la persona dietro la passione è una professionista certificata.
Output: Componente con 3 card credenziali, sfondo cream (alternanza con warm-white della storia), pronto per import in ChiSiamo.tsx.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md

@src/components/sections/PedigreeSection.tsx
@content/testi-sito-attuale/la-storia.md

<interfaces>
<!-- 3 credenziali da mostrare (da la-storia.md):
  1. Addestratrice ENCI
  2. Educatrice cinofila
  3. Master Allevatore ENCI

  Icone Lucide consigliate: GraduationCap, Award, Star (o Medal, BookMarked, BadgeCheck)
  Sfondo: var(--color-cream) per alternanza con warm-white della StoriaLaylaSection
-->

<!-- Pattern PedigreeSection per card con icona:
  - icona in cerchio background primary-pale
  - titolo Playfair Display
  - testo Poppins muted
  - contain: layout style paint
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare CredenzialiSection con 3 card ENCI</name>
  <files>src/components/sections/CredenzialiSection.tsx</files>
  <action>
Creare il componente `CredenzialiSection` con layout centrato: header sezione + nome Layla + griglia 3 card orizzontali.

**Struttura dati (array nel componente):**
```tsx
const credentials = [
  {
    icon: GraduationCap,
    title: 'Addestratrice ENCI',
    description: 'Titolo ufficiale riconosciuto dall\'Ente Nazionale della Cinofilia Italiana per l\'addestramento professionale dei cani.',
  },
  {
    icon: Award,
    title: 'Educatrice Cinofila',
    description: 'Specializzazione nella gestione comportamentale e nell\'educazione del cane nelle fasi di crescita.',
  },
  {
    icon: BadgeCheck,
    title: 'Master Allevatore ENCI',
    description: 'Il percorso formativo più avanzato dell\'ENCI per allevatori che operano con i più alti standard etici e sanitari.',
  },
]
```

**Struttura JSX:**
```tsx
<section className="cr-section" ref={sectionRef} aria-label="Credenziali di Layla Zarfati">
  <div className="cr-container" ref={containerRef}>
    <div className="cr-header">
      <span className="cr-badge">Le Credenziali</span>
      <h2 className="cr-title">
        <span className="cr-name">Layla Zarfati</span>
        <br />
        <em className="cr-subtitle-em">La professionista dietro la passione.</em>
      </h2>
      <p className="cr-intro">
        Anni di studio e formazione continua presso l'ENCI — l'Ente Nazionale della Cinofilia
        Italiana — a garanzia di un allevamento che rispetta i più alti standard etici e sanitari.
      </p>
    </div>
    <div className="cr-grid">
      {credentials.map((cred) => (
        <div key={cred.title} className="cr-card">
          <div className="cr-icon-wrap">
            <cred.icon size={28} strokeWidth={1.5} aria-hidden="true" />
          </div>
          <h3 className="cr-card-title">{cred.title}</h3>
          <p className="cr-card-text">{cred.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
```

**Animazioni:**
- `useEffect` IntersectionObserver bidirezionale su `containerRef`
- Aggiunge/rimuove classe `cr-entered`
- `requestAnimationFrame` pre-transition
- `threshold: 0, rootMargin: '-10% 0px'`
- Controlla `prefers-reduced-motion`: se ridotto, aggiunge `cr-entered` subito

**Import:** `useEffect`, `useRef` da react; `GraduationCap`, `Award`, `BadgeCheck` da lucide-react.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. Componente esporta `default CredenzialiSection`. Array credentials ha 3 elementi con tutti i campi.</done>
</task>

<task type="auto">
  <name>Task 2: CSS prefisso cr- in src/index.css</name>
  <files>src/index.css</files>
  <action>
Aggiungere in fondo a `src/index.css` (dopo i blocchi ls-) il CSS per `CredenzialiSection` con prefisso `cr-`.

**Regole da aggiungere:**

```css
/* ═══════════════════════════════════════════════════
   CREDENZIALI SECTION  (cr-)
   ═══════════════════════════════════════════════════ */

.cr-section {
  background: var(--color-cream);
  padding: clamp(4rem, 8vw, 7rem) 0;
}

.cr-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(1.5rem, 5vw, 4rem);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(2.5rem, 4vw, 3.5rem);
}

/* Header */
.cr-header {
  text-align: center;
  max-width: 640px;
}

.cr-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  margin-bottom: 1.25rem;
}

.cr-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-primary);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}

.cr-title {
  font-family: var(--font-heading);
  font-size: clamp(1.8rem, 3.2vw, 2.6rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.25;
  margin-bottom: 1rem;
}

.cr-name {
  color: var(--color-primary);
}

.cr-subtitle-em {
  font-style: italic;
  color: var(--color-text-muted);
  font-size: 0.75em;
}

.cr-intro {
  font-family: var(--font-body);
  font-size: clamp(0.875rem, 1.1vw, 1rem);
  color: var(--color-text-muted);
  line-height: 1.8;
}

/* Card grid */
.cr-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: clamp(1.25rem, 2.5vw, 2rem);
  width: 100%;
}

.cr-card {
  background: var(--color-warm-white);
  border-radius: var(--radius-card);
  padding: clamp(1.5rem, 2.5vw, 2rem);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 1rem;
  box-shadow: 0 2px 12px rgba(26, 16, 8, 0.06);
  contain: layout style paint;
}

.cr-icon-wrap {
  width: 64px;
  height: 64px;
  border-radius: 50%;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.cr-card-title {
  font-family: var(--font-heading);
  font-size: clamp(1rem, 1.3vw, 1.15rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.3;
}

.cr-card-text {
  font-family: var(--font-body);
  font-size: clamp(0.825rem, 0.95vw, 0.9rem);
  color: var(--color-text-muted);
  line-height: 1.7;
}

/* ── Animazione entrata ── */
.cr-container {
  opacity: 0;
  translate: 0 24px;
  transition:
    opacity 0.65s ease,
    translate 0.65s ease;
}

.cr-container.cr-entered {
  opacity: 1;
  translate: none;
}

.cr-container.cr-entered .cr-header { transition-delay: 0s; }
.cr-container.cr-entered .cr-grid  { transition-delay: 0.15s; }

/* ── Responsive ── */
@media (max-width: 768px) {
  .cr-grid {
    grid-template-columns: 1fr;
    max-width: 480px;
    margin: 0 auto;
  }
}

@media (min-width: 769px) and (max-width: 1024px) {
  .cr-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (min-width: 1440px) {
  .cr-container {
    padding: 0 3rem;
  }
}
```

Aggiungere dopo il blocco ls- già presente in index.css. Non modificare CSS esistente.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa. Classi cr- presenti in index.css. La sezione si visualizza con griglia 3 card su desktop e stack 1 colonna su mobile.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. Griglia 3 card visibile su desktop con icone Lucide in cerchio primary-pale
3. Stack 1 colonna su mobile (max-width 768px)
4. Nome "Layla Zarfati" in evidence con colore primary
5. IntersectionObserver: replay animazione al ri-scroll
</verification>

<success_criteria>
- CredenzialiSection.tsx esiste ed esporta il componente default
- CSS cr- in index.css senza conflitti
- Build production pulita (zero errori)
- 3 card: Addestratrice ENCI, Educatrice Cinofila, Master Allevatore ENCI
- Sfondo cream per alternanza visiva con warm-white della storia
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-02-SUMMARY.md`
</output>
