---
phase: 04-pagine-interne
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blogArticles.ts
  - src/swiper.d.ts
  - src/components/sections/BlogPreviewSection.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [BLOG-DATA, BLOG-PREVIEW-REFACTOR]

must_haves:
  truths:
    - "BlogArticle type definisce slug, title, date, category, excerpt, image, content, readingTime"
    - "Tutti i 15 articoli .md sono importati staticamente via ?raw e mappati nel data layer"
    - "BlogPreviewSection in homepage usa i dati dal data layer centralizzato, non dati hardcoded"
    - "I link delle card BlogPreview puntano a /blog/:slug (non a /blog generico)"
    - "react-markdown e installato come dipendenza"
  artifacts:
    - path: "src/data/blogArticles.ts"
      provides: "Data layer centralizzato blog con 15 articoli"
      exports: ["BlogArticle", "blogArticles", "blogArticlesSorted", "getArticleBySlug", "blogPreviewArticles"]
      min_lines: 120
    - path: "src/swiper.d.ts"
      provides: "Dichiarazione modulo *.md?raw per TypeScript"
      contains: "declare module"
  key_links:
    - from: "src/data/blogArticles.ts"
      to: "content/testi-sito-attuale/blog/*.md"
      via: "import ?raw statico"
      pattern: "import.*\\.md\\?raw"
    - from: "src/components/sections/BlogPreviewSection.tsx"
      to: "src/data/blogArticles.ts"
      via: "import blogPreviewArticles"
      pattern: "import.*blogPreviewArticles.*from.*blogArticles"
---

<objective>
Creare il data layer centralizzato per il blog e refactorare BlogPreviewSection per usarlo.

Purpose: Il data layer e il single source of truth per tutti i componenti blog (homepage preview, pagina griglia, pagina articolo). Senza di esso, i dati sono duplicati e i link non funzionano.
Output: `src/data/blogArticles.ts` con 15 articoli, BlogPreviewSection refactored, react-markdown installato.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pagine-interne/04-BLOG-RESEARCH.md

@src/components/sections/BlogPreviewSection.tsx
@content/testi-sito-attuale/blog/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installare react-markdown, creare type declaration ?raw, e data layer blog</name>
  <files>package.json, src/swiper.d.ts, src/data/blogArticles.ts</files>
  <action>
1. Installare react-markdown: `npm install react-markdown`

2. Aggiungere dichiarazione TypeScript per moduli `*.md?raw` — puo andare nel file `src/swiper.d.ts` esistente (rinominarlo in `src/env.d.ts` oppure aggiungere in coda):
```typescript
declare module '*.md?raw' {
  const content: string
  export default content
}
```

3. Creare `src/data/blogArticles.ts` con:

- Import statico di TUTTI i 15 file .md da `content/testi-sito-attuale/blog/` con suffix `?raw` (NON import dinamico — Vite non lo supporta in produzione). I file sono:
  - chi-alleva-alleva-tutto.md
  - controlli-si-grazie.md
  - facciamo-fare-il-cane-al-cane.md
  - il-barbone.md
  - il-coraggio-di-lasciarli-andare.md
  - lorigine-del-nome.md
  - maltipoo-cockapoo-o-goldendoodle.md
  - maschio-o-femmina.md
  - pandemia-sei-tutta-mia.md
  - per-una-zampata-di-fango.md
  - quando-a-volte-tornano.md
  - quando-cerchiamo-qualcuno-che-ci-scalda-il-cuore.md
  - siamo-tutti-allevatori.md
  - un-cucciolo-per-natale.md
  - va-dove-ti-porta-il-cane.md

- Esportare interfaccia `BlogArticle` con: slug, title, date (ISO YYYY-MM-DD), category, excerpt, image? (path in /content/images/), imageAlt?, imagePosition? (CSS object-position), content (raw markdown), readingTime (minuti)

- Helper `readingTime(text: string): number` — `Math.max(1, Math.round(words / 200))`

- Array `blogArticles: BlogArticle[]` con metadati per tutti i 15 articoli. Categorie da usare (dalla RESEARCH):
  - Allevamento: controlli-si-grazie
  - Storie: quando-a-volte-tornano, il-coraggio-di-lasciarli-andare, lorigine-del-nome
  - Riflessioni: chi-alleva-alleva-tutto, pandemia-sei-tutta-mia, quando-cerchiamo-qualcuno-che-ci-scalda-il-cuore, siamo-tutti-allevatori
  - Educazione: facciamo-fare-il-cane-al-cane, maltipoo-cockapoo-o-goldendoodle, maschio-o-femmina, un-cucciolo-per-natale
  - Vita quotidiana: per-una-zampata-di-fango, va-dove-ti-porta-il-cane
  - Razza: il-barbone

- Immagini (6 con copertina, path gia esistenti in public/content/images/):
  - controlli-si-grazie → maatilayla-blog-controlli-si-grazie.webp
  - quando-a-volte-tornano → maatilayla-blog-quando-tornano.webp
  - chi-alleva-alleva-tutto → maatilayla-blog-chi-alleva.webp
  - facciamo-fare-il-cane-al-cane → maatilayla-cucciolo-barboncino-toy-corre-prato.webp (con objectPosition 'center 0%')
  - per-una-zampata-di-fango → maatilayla-blog-zampata-fango.webp
  - pandemia-sei-tutta-mia → maatilayla-blog-pandemia.webp

- I 9 articoli senza immagine: NON impostare campo image (undefined). Il componente grid gestira il placeholder/fallback.

- Per excerpt: leggere ogni .md e scrivere un riassunto di 1-2 frasi che descrive il contenuto dell'articolo (come gia fatto per i 6 in BlogPreviewSection, completare i 9 mancanti).

- Per title: usare il titolo dal `# heading` del .md ma con accenti/apostrofi corretti (es. "Si, grazie!" diventa "Si, grazie!")

- Esportare:
  - `blogArticlesSorted` — ordinato per data decrescente
  - `getArticleBySlug(slug: string): BlogArticle | undefined`
  - `blogPreviewArticles` — primi 6 di blogArticlesSorted

- Esportare anche le categorie uniche: `blogCategories: string[]`
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>react-markdown installato, dichiarazione *.md?raw presente, blogArticles.ts esporta 15 articoli con type safety, `npm run build` passa</done>
</task>

<task type="auto">
  <name>Task 2: Refactorare BlogPreviewSection per usare il data layer centralizzato</name>
  <files>src/components/sections/BlogPreviewSection.tsx</files>
  <action>
1. Rimuovere l'array `articles` hardcoded in cima al componente

2. Importare `blogPreviewArticles` e il tipo `BlogArticle` da `@/data/blogArticles`

3. Usare `blogPreviewArticles` al posto di `articles` nel JSX

4. Aggiornare il rendering delle card:
   - `article.alt` diventa `article.imageAlt`
   - `article.objectPosition` diventa `article.imagePosition`
   - `article.date` ora e ISO (YYYY-MM-DD) — formattare con `new Date(article.date).toLocaleDateString('it-IT', { day: 'numeric', month: 'long', year: 'numeric' })`
   - Il `<Link to="/blog">` nella card diventa `<Link to={'/blog/' + article.slug}>`
   - L'`aria-label` usa `article.slug` o `article.title` per unicita

5. Gestire articoli senza immagine: usare fallback. Se `article.image` e undefined, non renderizzare il blocco `.bp-card-img` OPPURE usare una classe CSS per sfondo brand gradient (scelta a discrezione — l'importante e che non rompa il layout).

6. NON modificare: il layout, le animazioni, il drag-to-scroll, la progress bar, il fade — tutto il comportamento interattivo resta identico. Solo la fonte dati cambia.

7. Verificare che il componente funzioni identicamente in homepage — nessun cambiamento visivo.
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BlogPreviewSection usa blogPreviewArticles dal data layer, i link delle card puntano a /blog/:slug, build passa, homepage visivamente identica</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- `npx tsc --noEmit` passa senza errori TypeScript
- BlogPreviewSection in homepage mostra le stesse 6 card con stessi dati
- I link delle card blog puntano a `/blog/:slug` (non `/blog`)
- `react-markdown` presente in package.json dependencies
</verification>

<success_criteria>
- Data layer centralizzato con 15 articoli tipizzati e ordinati
- BlogPreviewSection refactored — zero duplicazione dati
- Link card funzionanti verso /blog/:slug
- Build pulita
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-08-SUMMARY.md`
</output>
