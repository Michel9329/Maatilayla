---
phase: 04-pagine-interne
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ValoriSection.tsx
  - src/index.css
autonomous: true
requirements: [CS-03]

must_haves:
  truths:
    - "L'utente vede 6 pillars dell'allevamento in una griglia 3×2 con icone Lucide"
    - "I pillars sono divisi in 2 gruppi visivi: 3 obiettivi cucciolo + 3 valori allevamento"
    - "Ogni card ha icona in cerchio primary-pale, titolo h3, 2 righe di testo descrittivo"
    - "L'animazione entrance IntersectionObserver bidirezionale funziona con stagger sulle card"
  artifacts:
    - path: "src/components/sections/ValoriSection.tsx"
      provides: "Sezione 6 pillars filosofia allevamento"
      exports: ["default ValoriSection"]
    - path: "src/index.css"
      provides: "CSS prefisso vl- per ValoriSection"
      contains: ".vl-section"
  key_links:
    - from: "src/components/sections/ValoriSection.tsx"
      to: "src/index.css"
      via: "classi CSS vl-"
      pattern: "vl-section|vl-card|vl-entered"
---

<objective>
Costruire ValoriSection: griglia 3×2 con i 6 pillars della filosofia Maatilayla.

Purpose: Terza sezione della pagina Chi Siamo. Dopo emozione (storia) e credibilità (credenziali), questa sezione comunica i valori concreti — i principi che guidano ogni scelta dell'allevamento.
Output: Componente con 2 gruppi da 3 card ciascuno, sfondo warm-white, pronto per import in ChiSiamo.tsx.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md

@src/components/sections/PedigreeSection.tsx
@content/testi-sito-attuale/chi-siamo.md

<interfaces>
<!-- 6 pillars da chi-siamo.md:
  Gruppo 1 — I tre obiettivi (focus cucciolo):
  1. Cuccioli sani — "Scrupolosi controlli sanitari su riproduttori e cuccioli, per scongiurare patologie cardiologiche e le principali malattie ereditarie."
  2. Cuccioli belli — "Selezione attenta dei riproduttori per ottenere cuccioli sempre più meritevoli, nel rispetto dello standard di razza."
  3. Cuccioli equilibrati — "Tecnica Biosensor e permanenza in allevamento fino a 3 mesi: gli strumenti giusti per un percorso di vita sereno."

  Gruppo 2 — I tre valori (focus allevamento):
  4. Rispetto della natura — "Accoppiamenti naturali e intervallo di almeno un anno tra una gravidanza e l'altra, per il benessere della fattrice."
  5. Ambiente sereno — "I cuccioli crescono a contatto con altre specie animali — gatti, conigli, tartarughe — sviluppando rispetto e sicurezza."
  6. Punto di riferimento — "Anche dopo la cessione siamo sempre disponibili: consigli, supporto e tutta la nostra esperienza per chi ne avesse bisogno."

  Icone Lucide consigliate:
  1. ShieldCheck (sani)
  2. Sparkles (belli)
  3. Heart (equilibrati)
  4. Leaf (rispetto natura)
  5. Sun (ambiente sereno)
  6. HandHeart (punto di riferimento)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare ValoriSection con griglia 3×2 divisa in 2 gruppi</name>
  <files>src/components/sections/ValoriSection.tsx</files>
  <action>
Creare il componente `ValoriSection` con header centrato + 2 gruppi di 3 card in griglia 3 colonne.

**Struttura dati (array nel componente):**
```tsx
const groups = [
  {
    label: 'I Tre Obiettivi',
    pillars: [
      {
        icon: ShieldCheck,
        title: 'Cuccioli sani.',
        text: 'Scrupolosi controlli sanitari su riproduttori e cuccioli, per scongiurare patologie cardiologiche e le principali malattie ereditarie.',
      },
      {
        icon: Sparkles,
        title: 'Cuccioli belli.',
        text: 'Selezione attenta dei riproduttori per ottenere cuccioli sempre più meritevoli, nel rispetto dello standard di razza.',
      },
      {
        icon: Heart,
        title: 'Cuccioli equilibrati.',
        text: 'Tecnica Biosensor e permanenza in allevamento fino a 3 mesi: gli strumenti giusti per un percorso di vita sereno.',
      },
    ],
  },
  {
    label: 'I Tre Valori',
    pillars: [
      {
        icon: Leaf,
        title: 'Rispetto della natura.',
        text: 'Accoppiamenti naturali e intervallo di almeno un anno tra una gravidanza e l\'altra, per il benessere della fattrice.',
      },
      {
        icon: Sun,
        title: 'Ambiente sereno.',
        text: 'I cuccioli crescono a contatto con gatti, conigli e tartarughe — sviluppando rispetto per le altre specie e sicurezza emotiva.',
      },
      {
        icon: HandHeart,
        title: 'Punto di riferimento.',
        text: 'Anche dopo la cessione siamo sempre disponibili: consigli, supporto e tutta la nostra esperienza per chi ne avesse bisogno.',
      },
    ],
  },
]
```

**Struttura JSX:**
```tsx
<section className="vl-section" ref={sectionRef} aria-label="I nostri valori">
  <div className="vl-container" ref={containerRef}>
    <div className="vl-header">
      <span className="vl-badge">Filosofia e Valori</span>
      <h2 className="vl-title">
        Tre obiettivi. Tre valori. <em className="vl-accent">Una sola missione.</em>
      </h2>
      <p className="vl-intro">
        Ogni barboncino toy nato a Maatilayla è il risultato di scelte precise, guidate dai
        principi che ci appartengono da sempre.
      </p>
    </div>

    {groups.map((group) => (
      <div key={group.label} className="vl-group">
        <span className="vl-group-label">{group.label}</span>
        <div className="vl-grid">
          {group.pillars.map((pillar) => (
            <div key={pillar.title} className="vl-card">
              <div className="vl-icon-wrap">
                <pillar.icon size={26} strokeWidth={1.5} aria-hidden="true" />
              </div>
              <h3 className="vl-card-title">{pillar.title}</h3>
              <p className="vl-card-text">{pillar.text}</p>
            </div>
          ))}
        </div>
      </div>
    ))}
  </div>
</section>
```

**Animazioni:**
- `useEffect` IntersectionObserver bidirezionale su `containerRef`
- Aggiunge/rimuove classe `vl-entered`
- `requestAnimationFrame` pre-transition
- `threshold: 0, rootMargin: '-10% 0px'`
- Controlla `prefers-reduced-motion`: se ridotto, aggiunge `vl-entered` subito senza IntersectionObserver

**Import:** `useEffect`, `useRef` da react; `ShieldCheck`, `Sparkles`, `Heart`, `Leaf`, `Sun`, `HandHeart` da lucide-react.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. Componente esporta `default ValoriSection`. Due gruppi con 3 card ciascuno renderizzati correttamente.</done>
</task>

<task type="auto">
  <name>Task 2: CSS prefisso vl- in src/index.css</name>
  <files>src/index.css</files>
  <action>
Aggiungere in fondo a `src/index.css` (dopo i blocchi cr-) il CSS per `ValoriSection` con prefisso `vl-`.

**Regole da aggiungere:**

```css
/* ═══════════════════════════════════════════════════
   VALORI SECTION  (vl-)
   ═══════════════════════════════════════════════════ */

.vl-section {
  background: var(--color-warm-white);
  padding: clamp(4rem, 8vw, 7rem) 0;
}

.vl-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(1.5rem, 5vw, 4rem);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(2.5rem, 4vw, 3.5rem);
}

/* Header */
.vl-header {
  text-align: center;
  max-width: 660px;
}

.vl-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  margin-bottom: 1.25rem;
}

.vl-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-primary);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}

.vl-title {
  font-family: var(--font-heading);
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.25;
  margin-bottom: 1rem;
}

.vl-accent {
  font-style: italic;
  color: var(--color-primary);
}

.vl-intro {
  font-family: var(--font-body);
  font-size: clamp(0.875rem, 1.1vw, 1rem);
  color: var(--color-text-muted);
  line-height: 1.8;
}

/* Gruppo */
.vl-group {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.vl-group-label {
  font-family: var(--font-body);
  font-size: 0.7rem;
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-secondary);
  padding-bottom: 0.5rem;
  border-bottom: 1px solid var(--color-primary-pale);
}

/* Card grid */
.vl-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: clamp(1rem, 2vw, 1.75rem);
}

.vl-card {
  background: var(--color-cream);
  border-radius: var(--radius-card);
  padding: clamp(1.5rem, 2vw, 1.875rem);
  display: flex;
  flex-direction: column;
  gap: 0.875rem;
  contain: layout style paint;
}

.vl-icon-wrap {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.vl-card-title {
  font-family: var(--font-heading);
  font-size: clamp(1rem, 1.2vw, 1.1rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.3;
}

.vl-card-text {
  font-family: var(--font-body);
  font-size: clamp(0.825rem, 0.95vw, 0.9rem);
  color: var(--color-text-muted);
  line-height: 1.7;
}

/* ── Animazione entrata ── */
.vl-container {
  opacity: 0;
  translate: 0 24px;
  transition:
    opacity 0.65s ease,
    translate 0.65s ease;
}

.vl-container.vl-entered {
  opacity: 1;
  translate: none;
}

.vl-container.vl-entered .vl-header    { transition-delay: 0s; }
.vl-container.vl-entered .vl-group:nth-child(2) { transition-delay: 0.12s; }
.vl-container.vl-entered .vl-group:nth-child(3) { transition-delay: 0.24s; }

/* ── Responsive ── */
@media (max-width: 768px) {
  .vl-grid {
    grid-template-columns: 1fr;
  }
}

@media (min-width: 769px) and (max-width: 1024px) {
  .vl-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (min-width: 1440px) {
  .vl-container {
    padding: 0 3rem;
  }
}
```

Aggiungere dopo il blocco cr- già presente in index.css. Non modificare CSS esistente.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa. Classi vl- presenti in index.css. Due gruppi con griglia 3×2 visibili su desktop. Mobile stack 1 colonna.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. Due gruppi visivi distinti: "I Tre Obiettivi" e "I Tre Valori"
3. 6 card totali con icone Lucide in cerchio primary-pale
4. Sfondo warm-white per alternanza con cream della CredenzialiSection
5. IntersectionObserver: replay animazione al ri-scroll
</verification>

<success_criteria>
- ValoriSection.tsx esiste ed esporta il componente default
- CSS vl- in index.css senza conflitti
- Build production pulita (zero errori)
- 6 pillars da chi-siamo.md, divisi in 2 gruppi con separatore visivo
- Testi italiani coerenti con il tono della pagina
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-03-SUMMARY.md`
</output>
