---
phase: 04-pagine-interne
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - public/content/images/maatilayla-header-blog.webp
  - src/pages/Blog.tsx
  - src/components/sections/BlogCta.tsx
  - src/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "La pagina /blog mostra una nuova foto hero processata (maatilayla-header-blog.webp)"
    - "Prima di ContactSection appare una CTA con foto background che punta a /chi-siamo"
    - "Le pill categoria nella griglia blog usano verde salvia (#8BAD91) come accento"
  artifacts:
    - path: "public/content/images/maatilayla-header-blog.webp"
      provides: "Foto hero blog processata con sharp"
    - path: "src/components/sections/BlogCta.tsx"
      provides: "CTA sezione blog con pattern ChiSiamoCta"
      min_lines: 60
    - path: "src/index.css"
      provides: "CSS bc- per BlogCta + verde salvia pill"
      contains: ".bc-section"
  key_links:
    - from: "src/pages/Blog.tsx"
      to: "src/components/sections/BlogCta.tsx"
      via: "import e rendering tra BlogGrid e ContactSection"
      pattern: "BlogCta"
    - from: "src/pages/Blog.tsx"
      to: "public/content/images/maatilayla-header-blog.webp"
      via: "HeroSection image prop"
      pattern: "maatilayla-header-blog"
---

<objective>
Aggiornare la pagina Blog griglia: processare la nuova foto hero AB27, creare la sezione BlogCta (pattern ChiSiamoCta) prima di ContactSection, e introdurre verde salvia sulle pill categoria.

Purpose: Elevare la pagina blog con hero fotografico dedicato e CTA che guida verso Chi Siamo, riducendo la dominanza arancione con accenti verdi.
Output: Foto processata, BlogCta.tsx, Blog.tsx aggiornato, CSS bc-.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pagine-interne/04-BLOG-REWORK-CONTEXT.md

@src/pages/Blog.tsx
@src/components/sections/ChiSiamoCta.tsx
@src/index.css

<interfaces>
<!-- Pattern CTA da replicare (ChiSiamoCta) -->
From src/components/sections/ChiSiamoCta.tsx:
```typescript
// Struttura: .cs-wrap > section.cs-section > .cs-content + img.cs-bg
// Badge (cs-badge) + Title word-by-word (cs-word) + Body (cs-body) + CTA button (cs-cta-btn)
// ScrollTrigger desktop, IntersectionObserver mobile
// CSS classes prefisso cs- -> per BlogCta usare bc-
```

From src/pages/Blog.tsx:
```typescript
// Ordine attuale: HeroSection > BlogGrid > ContactSection > InstagramFeedSection
// BlogCta va inserito TRA BlogGrid e ContactSection
```

Colore verde salvia: #8BAD91 (gia' usato in ContactSection Chi Siamo, AllevamentoSection pills)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Processare foto hero + aggiornare Blog.tsx</name>
  <files>public/content/images/maatilayla-header-blog.webp, src/pages/Blog.tsx</files>
  <action>
1. Processare la foto sorgente con sharp:
   - Sorgente: `D:/Users/miche/Download/barboni/barboni1/AB27EBFC-BD02-4411-B276-013005E4A521.jpeg`
   - Resize max width 1600px (hero background)
   - Quality 78-80 webp
   - Output: `public/content/images/maatilayla-header-blog.webp`
   - Script inline node: `sharp(input).resize({width:1600,withoutEnlargement:true}).webp({quality:79}).toFile(output)`

2. Aggiornare `src/pages/Blog.tsx`:
   - Cambiare HeroSection `image` prop da `maatilayla-header-cucciolo-allevamento.webp` a `maatilayla-header-blog.webp`
   - Aggiornare `alt` con descrizione appropriata della nuova foto
   - Aggiornare OG image meta tag con il nuovo path
   - Importare e inserire `BlogCta` tra `BlogGrid` e `ContactSection`

3. Applicare verde salvia alle pill categoria nella griglia blog:
   - In `src/index.css`, modificare `.bg-card-category` da usare #8BAD91 come background (con opacita' tipo rgba(139,173,145,0.12)) e #5A8C62 come testo
   - Mantenere hover/active states coerenti
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Foto hero processata sotto 200KB, Blog.tsx usa nuova foto, pill categoria verdi, BlogCta importato e renderizzato</done>
</task>

<task type="auto">
  <name>Task 2: Creare BlogCta + CSS bc-</name>
  <files>src/components/sections/BlogCta.tsx, src/index.css</files>
  <action>
1. Creare `src/components/sections/BlogCta.tsx` clonando il pattern di ChiSiamoCta.tsx:
   - Wrapper `.bc-wrap` con padding e warm-white background
   - Section `.bc-section` con max-width 1400px, border-radius var(--radius-card), min-height 420px, overflow hidden, position relative
   - Foto background: riusare una foto esistente (es. `maatilayla-cta-chi-siamo-background.webp` o altra foto disponibile dei barboncini) con `.bc-bg` (object-fit cover, position absolute, z-index 0)
   - Overlay scuro semi-trasparente per leggibilita' testo
   - Content `.bc-content` posizionato sopra (z-index 1):
     - Badge `.bc-badge`: "Chi Siamo" (con dot pulse verde salvia)
     - Titolo `.bc-title` word-by-word: "Scopri la nostra storia." con "storia." come accent word
     - Body `.bc-body`: "Dietro ogni cucciolo c'e' una famiglia, una filosofia e anni di dedizione. Conoscici meglio."
     - CTA `.bc-cta-btn`: "Scopri chi siamo" -> Link to="/chi-siamo"
   - Desktop: ScrollTrigger + GSAP word-by-word (stessa logica ChiSiamoCta)
   - Mobile/reduced-motion: IntersectionObserver CSS fade (classe `bc-entered`)
   - Parallax disabilitato (come ChiSiamoCta — evita bordi visibili)
   - prefers-reduced-motion: tutto visibile, no animazione
   - lightText={true} style (testo bianco su foto scura)

2. CSS in `src/index.css` con prefisso `bc-`:
   - `.bc-wrap`: padding 2rem 1.5rem, background var(--color-warm-white)
   - `.bc-section`: max-width 1400px, margin 0 auto, border-radius var(--radius-card), min-height 420px, position relative, overflow hidden, display flex, align-items center
   - `.bc-bg`: position absolute, inset 0, object-fit cover, width/height 100%
   - `.bc-overlay`: position absolute, inset 0, background rgba(0,0,0,0.45)
   - `.bc-content`: position relative, z-index 1, max-width 520px, padding 3rem, gap 1.25rem, display flex, flex-direction column
   - `.bc-badge`: font-size 0.65rem, font-weight 600, text-transform uppercase, color white, con dot prima (background verde salvia #8BAD91 con pulse-dot animation)
   - `.bc-title`: clamp(1.8rem, 3vw, 2.8rem), font-weight 700, line-height 1.15, color white, font-family var(--font-heading)
   - `.bc-word`: display inline-block (per GSAP)
   - `.bc-word--accent em`: color var(--color-primary-light), font-style normal
   - `.bc-body`: clamp(0.9rem, 1.1vw, 1.05rem), line-height 1.65, color rgba(255,255,255,0.85)
   - `.bc-cta-btn`: inline-block, padding 0.75rem 2rem, border-radius var(--radius-btn), background var(--color-primary), color white, font-weight 600, font-size 0.9rem, transition, hover translateY(-1px) + primary-light bg
   - Transizioni entrata (opacity, transform) su `.bc-section.bc-entered .bc-content`
   - Responsive mobile (padding ridotto, min-height 360px)
  </action>
  <verify>
    <automated>cd C:/Users/miche/Desktop/Maatilayla && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BlogCta renderizzato con foto background, word-by-word GSAP desktop, fade mobile, punta a /chi-siamo</done>
</task>

</tasks>

<verification>
- `npm run build` passa senza errori
- Foto hero sotto 200KB in formato webp
- BlogCta visibile tra griglia e ContactSection
- Pill categoria blog in verde salvia
</verification>

<success_criteria>
Pagina /blog ha: nuova foto hero dedicata, pill categoria verdi, CTA "Scopri chi siamo" con foto background prima della sezione contatti.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pagine-interne/04-13-SUMMARY.md`
</output>
