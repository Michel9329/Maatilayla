---
phase: 04-pagine-interne
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - src/components/sections/WorldMapSection.tsx
  - src/data/wm-destinations.json
  - public/content/data/countries-110m.json
  - src/index.css
autonomous: true
requirements: [CS-06]

must_haves:
  truths:
    - "L'utente vede una mappa del mondo SVG stilizzata nei colori del brand"
    - "I paesi dove sono andati cuccioli Maatilayla hanno un marker punto rosso primary"
    - "Il tooltip/title sul marker mostra il nome del paese al hover"
    - "La mappa funziona con i dati placeholder (2-3 paesi) senza errori"
    - "L'animazione entrance IntersectionObserver bidirezionale funziona"
  artifacts:
    - path: "src/components/sections/WorldMapSection.tsx"
      provides: "Mappa SVG interattiva cuccioli Maatilayla nel mondo"
      exports: ["default WorldMapSection"]
    - path: "src/data/wm-destinations.json"
      provides: "Dati configurabili destinazioni cuccioli"
      contains: "coordinates"
    - path: "public/content/data/countries-110m.json"
      provides: "GeoJSON paesi del mondo per la mappa"
    - path: "src/index.css"
      provides: "CSS prefisso wm- per WorldMapSection"
      contains: ".wm-section"
  key_links:
    - from: "src/components/sections/WorldMapSection.tsx"
      to: "src/data/wm-destinations.json"
      via: "import JSON diretto"
      pattern: "wm-destinations"
    - from: "src/components/sections/WorldMapSection.tsx"
      to: "public/content/data/countries-110m.json"
      via: "prop geography di Geographies"
      pattern: "countries-110m"
---

<objective>
Costruire WorldMapSection: mappa SVG interattiva con marker per ogni paese dove sono andati cuccioli Maatilayla.

Purpose: Sesta sezione della pagina Chi Siamo. Momento "wow" che chiude il racconto narrativo — i cuccioli Maatilayla sono amati in tutto il mondo. Feature differenziante del sito.
Output: Componente con @vnedyalk0v/react19-simple-maps installato, dati placeholder configurabili in JSON, CSS in index.css.
</objective>

<execution_context>
@C:/Users/miche/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/miche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pagine-interne/04-CONTEXT.md
@.planning/phases/04-pagine-interne/04-RESEARCH.md

<interfaces>
<!-- Libreria: @vnedyalk0v/react19-simple-maps v2.0.2 (React 19 compatible fork) -->
<!-- API verificata in 04-RESEARCH.md:
  import { ComposableMap, Geographies, Geography, Marker } from '@vnedyalk0v/react19-simple-maps'

  <ComposableMap projection="geoEqualEarth" projectionConfig={{ scale: 147 }} width={800} height={400}>
    <Geographies geography="/content/data/countries-110m.json">
      {({ geographies }) => geographies.map((geo) => (
        <Geography key={geo.rsmKey} geography={geo} fill="var(--color-accent)" stroke="var(--color-cream)" strokeWidth={0.5} />
      ))}
    </Geographies>
    <Marker coordinates={[longitude, latitude]}>
      <circle r={5} fill="var(--color-primary)" stroke="#fff" strokeWidth={1} />
      <title>Nome paese</title>
    </Marker>
  </ComposableMap>

  ATTENZIONE: coordinates = [longitude, latitude] (non lat/lon!)
-->

<!-- Struttura JSON da 04-RESEARCH.md:
  [{ "id": "it", "name": "Italia", "coordinates": [12.4964, 41.9028], "count": 3 }]
  File: src/data/wm-destinations.json (importato come JSON, non public/)
-->

<!-- GeoJSON da scaricare:
  curl -o public/content/data/countries-110m.json https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json
  Zero dipendenza CDN a runtime — file locale
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup — npm install libreria, download GeoJSON, creare JSON placeholder</name>
  <files>src/data/wm-destinations.json, public/content/data/countries-110m.json</files>
  <action>
Eseguire in sequenza:

**1. Installare la libreria React 19 compatibile:**
```bash
npm install @vnedyalk0v/react19-simple-maps
```

**2. Scaricare il file GeoJSON dei paesi del mondo (zero CDN a runtime):**
```bash
curl -o public/content/data/countries-110m.json https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json
```
Se `curl` non è disponibile, usare `node -e "require('https').get('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json', r => { let d=''; r.on('data',c=>d+=c); r.on('end',()=>require('fs').writeFileSync('public/content/data/countries-110m.json',d)) })"`.

**3. Creare `src/data/wm-destinations.json`** con placeholder (i dati reali arriveranno dall'utente dopo):
```json
[
  {
    "id": "it",
    "name": "Italia",
    "coordinates": [12.4964, 41.9028],
    "count": 5
  },
  {
    "id": "fr",
    "name": "Francia",
    "coordinates": [2.3522, 48.8566],
    "count": 2
  },
  {
    "id": "ch",
    "name": "Svizzera",
    "coordinates": [8.2275, 46.8182],
    "count": 1
  },
  {
    "id": "de",
    "name": "Germania",
    "coordinates": [10.4515, 51.1657],
    "count": 1
  }
]
```

**Struttura del JSON spiegata:**
- `id`: codice paese ISO (per futura espansione, es. highlight paese)
- `name`: nome italiano del paese (visualizzato nel tooltip `<title>`)
- `coordinates`: [longitudine, latitudine] — ordine obbligatorio per react-simple-maps
- `count`: numero cuccioli in quel paese (visibile in tooltip, utile per dimensionare marker in futuro)

Verificare che `public/content/data/` esista — creare se necessario.
  </action>
  <verify>
    <automated>ls public/content/data/countries-110m.json && cat src/data/wm-destinations.json | head -5</automated>
  </verify>
  <done>File countries-110m.json esiste in public/content/data/. File wm-destinations.json esiste in src/data/ con almeno 3 destinazioni placeholder. Libreria @vnedyalk0v/react19-simple-maps in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Creare WorldMapSection e CSS wm- in src/index.css</name>
  <files>src/components/sections/WorldMapSection.tsx, src/index.css</files>
  <action>
**Parte A — Componente WorldMapSection.tsx:**

```tsx
import { useEffect, useRef } from 'react'
import { ComposableMap, Geographies, Geography, Marker } from '@vnedyalk0v/react19-simple-maps'
import destinations from '@/data/wm-destinations.json'

const GEO_URL = '/content/data/countries-110m.json'

interface Destination {
  id: string
  name: string
  coordinates: [number, number]
  count: number
}

const data = destinations as Destination[]

export default function WorldMapSection() {
  const sectionRef = useRef<HTMLElement>(null)
  const containerRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (prefersReduced) {
      containerRef.current?.classList.add('wm-entered')
      return
    }
    const observer = new IntersectionObserver(
      ([entry]) => {
        requestAnimationFrame(() => {
          if (entry.isIntersecting) {
            containerRef.current?.classList.add('wm-entered')
          } else {
            containerRef.current?.classList.remove('wm-entered')
          }
        })
      },
      { threshold: 0, rootMargin: '-10% 0px' },
    )
    if (containerRef.current) observer.observe(containerRef.current)
    return () => observer.disconnect()
  }, [])

  return (
    <section className="wm-section" ref={sectionRef} aria-label="Cuccioli Maatilayla nel mondo">
      <div className="wm-container" ref={containerRef}>
        <div className="wm-header">
          <span className="wm-badge">Nel Mondo</span>
          <h2 className="wm-title">
            I cuccioli Maatilayla, <em className="wm-accent">ovunque nel mondo.</em>
          </h2>
          <p className="wm-intro">
            Ogni punto sulla mappa è una famiglia che ha scelto di portare un barboncino toy
            Maatilayla nella propria vita. Una storia di passione che va oltre i confini.
          </p>
        </div>

        <div className="wm-map-wrap">
          <ComposableMap
            projection="geoEqualEarth"
            projectionConfig={{ scale: 147 }}
            width={800}
            height={400}
            style={{ width: '100%', height: 'auto' }}
          >
            <Geographies geography={GEO_URL}>
              {({ geographies }) =>
                geographies.map((geo) => (
                  <Geography
                    key={geo.rsmKey}
                    geography={geo}
                    fill="var(--color-accent)"
                    stroke="var(--color-cream)"
                    strokeWidth={0.5}
                    style={{
                      default: { outline: 'none' },
                      hover: { outline: 'none', fill: 'var(--color-secondary)' },
                      pressed: { outline: 'none' },
                    }}
                  />
                ))
              }
            </Geographies>

            {data.map((dest) => (
              <Marker key={dest.id} coordinates={dest.coordinates}>
                <circle
                  r={6}
                  fill="var(--color-primary)"
                  stroke="#fff"
                  strokeWidth={1.5}
                  style={{ cursor: 'default' }}
                />
                <title>{dest.name} — {dest.count} {dest.count === 1 ? 'cucciolo' : 'cuccioli'}</title>
              </Marker>
            ))}
          </ComposableMap>
        </div>

        <p className="wm-caption">
          I dati sono in aggiornamento continuo con ogni nuova adozione.
        </p>
      </div>
    </section>
  )
}
```

**Parte B — CSS wm- in src/index.css:**

Aggiungere in fondo a index.css (dopo il blocco tl-):

```css
/* ═══════════════════════════════════════════════════
   WORLD MAP SECTION  (wm-)
   ═══════════════════════════════════════════════════ */

.wm-section {
  background: var(--color-cream);
  padding: clamp(4rem, 8vw, 7rem) 0;
}

.wm-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(1.5rem, 5vw, 4rem);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(2rem, 3.5vw, 3rem);
}

/* Header */
.wm-header {
  text-align: center;
  max-width: 640px;
}

.wm-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  padding: 0.35rem 0.85rem;
  border-radius: 2rem;
  margin-bottom: 1.25rem;
}

.wm-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-primary);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}

.wm-title {
  font-family: var(--font-heading);
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.25;
  margin-bottom: 1rem;
}

.wm-accent {
  font-style: italic;
  color: var(--color-primary);
}

.wm-intro {
  font-family: var(--font-body);
  font-size: clamp(0.875rem, 1.1vw, 1rem);
  color: var(--color-text-muted);
  line-height: 1.8;
}

/* Mappa */
.wm-map-wrap {
  width: 100%;
  max-width: 1000px;
  background: var(--color-warm-white);
  border-radius: var(--radius-card);
  padding: 1.5rem;
  box-shadow: 0 2px 20px rgba(26, 16, 8, 0.06);
  contain: layout style;
}

.wm-caption {
  font-family: var(--font-body);
  font-size: 0.78rem;
  color: var(--color-text-muted);
  text-align: center;
  font-style: italic;
}

/* ── Animazione entrata ── */
.wm-container {
  opacity: 0;
  translate: 0 24px;
  transition:
    opacity 0.65s ease,
    translate 0.65s ease;
}

.wm-container.wm-entered {
  opacity: 1;
  translate: none;
}

.wm-container.wm-entered .wm-header    { transition-delay: 0s; }
.wm-container.wm-entered .wm-map-wrap  { transition-delay: 0.15s; }
.wm-container.wm-entered .wm-caption   { transition-delay: 0.25s; }

/* ── Responsive ── */
@media (max-width: 768px) {
  .wm-map-wrap {
    padding: 0.75rem;
    border-radius: var(--radius-card);
  }
}

@media (min-width: 1440px) {
  .wm-container {
    padding: 0 3rem;
  }
}
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passa zero errori TypeScript. Componente WorldMapSection esporta default. La mappa SVG si renderizza con paesi in colore accent e marker rossi sui paesi placeholder.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errori TypeScript e zero errori ESLint
2. `npm run dev` — aprire /chi-siamo (dopo assembly Plan 07): mappa SVG visibile con continenti in beige/caramello e dot rossi su Italia, Francia, Svizzera, Germania
3. Hover su marker: tooltip nativo del browser mostra nome paese e numero cuccioli
4. Nessun errore console relativo a GeoJSON o react-simple-maps
5. File countries-110m.json servito come asset statico da /content/data/
</verification>

<success_criteria>
- WorldMapSection.tsx esiste ed esporta il componente default
- @vnedyalk0v/react19-simple-maps installato in package.json
- public/content/data/countries-110m.json presente (GeoJSON completo)
- src/data/wm-destinations.json con struttura [{ id, name, coordinates: [lon, lat], count }]
- CSS wm- in index.css senza conflitti
- Build production pulita (zero errori)
- Mappa funzionante con colori brand (paesi: --color-accent, markers: --color-primary)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-pagine-interne/04-06-SUMMARY.md`
</output>
